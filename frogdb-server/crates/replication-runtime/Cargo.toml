[package]
name = "frogdb-replication-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Server-side replication runtime: replica apply, full-resync export/install, replica-loss write fence"

[dependencies]
frogdb-config.workspace = true
frogdb-core.workspace = true
frogdb-protocol.workspace = true
frogdb-replication.workspace = true
bytes.workspace = true
tokio.workspace = true
tracing.workspace = true

[dev-dependencies]
# The self-fence admission property (`src/properties.rs`). Dev-only: it drives
# the production checker, it is not part of the shipped crate.
proptest.workspace = true
