# Cross-compilation configuration for cross-rs
# Used to build Linux x86_64 binaries from macOS

[target.x86_64-unknown-linux-gnu]
pre-build = [
    "apt-get update && apt-get install -y libclang-dev clang cmake"
]

[target.x86_64-unknown-linux-gnu.env]
passthrough = ["LIBCLANG_PATH"]
