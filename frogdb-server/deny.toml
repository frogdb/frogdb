[graph]
all-features = false
no-default-features = false

[advisories]
ignore = [
    # paste is unmaintained but only a transitive dev-dep via thirtyfour (browser tests).
    # No fix available until thirtyfour migrates to pastey.
    { id = "RUSTSEC-2024-0436", reason = "dev-only transitive dep, no fix available" },
]

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "Unicode-3.0",
    "CDLA-Permissive-2.0",
    "BSL-1.0",
]
confidence-threshold = 0.8

[licenses.private]
ignore = true

[bans]
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
