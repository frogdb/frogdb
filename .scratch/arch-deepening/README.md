# arch-deepening — architecture deepening rounds

State: active

Rolling architecture-quality rounds: proposals get written, triaged, and the accepted ones
become issues. The **proposals** backlog is empty (round 10 and the follow-up rounds all
landed, `origin/main` ff7861d5 / d14a7323 / c2878ffb). The **issues** backlog is not — 15
implementation issues remain open.

## Layout

| path | what |
|---|---|
| `proposals/` | round proposals + `INDEX.md`. Backlog empty; kept as the record |
| `issues/open/` | 15 open |
| `issues/done/` | 5 closed |

Issues `13-01`/`13-02`/`13-03` are sub-issues of 13, not a single issue — cite them with the
full number.

## Inbound references

None outside `.scratch/`.
