# Proposal 46 — One `VllAcquireError`, one reply mapping

## Summary

`frogdb-vll` exposes two error types for the same thing. `ScatterError`
(coordinator.rs:74–87) and `ContinuationError` (coordinator.rs:148–153) describe the
failure of a VLL *lock acquisition*; `ContinuationError`'s four variants are
field-for-field identical to `ScatterError`'s first four. Each has its own
`Display` (coordinator.rs:89–110, 155–173) and — the part that matters — its own
translator to the client's reply, in a different crate: `scatter_error_to_response`
(`server/src/scatter/executor.rs:141–193`) and `continuation_error_to_response`
(`server/src/connection/scripting/eval.rs:268–281`).

Two adapters exist for a seam that has only one meaning, and they have drifted. Of
the seven conditions both can see, **one produces the same reply and six do not**.
The consequential one is `VllError::ShardBusy`: the scatter path answers
`-BUSY shard busy with continuation lock; retry` (executor.rs:155–157) and the
continuation path answers `-ERR lock acquisition failed: Shard busy with
continuation lock` (eval.rs:271–273, no `ShardBusy` arm). Same shard state, same
`ShardReadyResult::Failed(ShardBusy)`, same coordinator — and a client's retry
policy keys off the code. Enter through `MGET` and the condition is transient;
enter through cross-shard `EVAL` and it is permanent. The website glossary already
documents the intended behavior for both entry paths
(`website/src/content/docs/architecture/glossary.md:144`: "A lock request hit a
shard held by a Lua/MULTI continuation lock" → `-BUSY`); it is the code that
disagrees with itself.

This proposal collapses both types into one `VllAcquireError` whose reply mapping
lives once, in `frogdb-vll`. That is spec-first work: `FM-VLL-001`–`004` pin the
current strings, so the row edits and the failing tests come before the fix. The
move also drags the mapping inside the 0.90 mutation gate — today a mutant that
flips `BUSY` to `ERR` is not generated by *any* gated run, which is precisely the
complaint in campaign-2 issue 10.

## Files involved

| Path | Lines | Role |
|------|-------|------|
| `frogdb-server/crates/vll/src/coordinator.rs` | 886 | Both error types (`ScatterError` 74–87, `ContinuationError` 148–153), both `Display`s (89–110, 155–173), and the two producers (`scatter` 208–310, `acquire_continuation` 361–413). **Owned by this proposal.** |
| `frogdb-server/crates/vll/src/lib.rs` | 28 | Re-export surface (line 19) — `ContinuationError` disappears, `VllAcquireError` appears |
| `frogdb-server/crates/vll/Cargo.toml` | 16 | Declares `frogdb-protocol` (line 12), **used nowhere in the crate today** — the dependency edge the new mapping needs already exists |
| `frogdb-server/crates/server/src/scatter/executor.rs` | 194 | `scatter_error_to_response` (141–193), 6 arms / 7 replies. Keeps its `warn!`s, loses its string table |
| `frogdb-server/crates/server/src/connection/scripting/eval.rs` | 281 | `continuation_error_to_response` (268–281), 4 arms, **no logging at all**; sole call site at 167. **Only these two regions are owned here** — see sibling boundaries |
| `frogdb-server/crates/shard-harness/tests/scenario_s4.rs` | — | `Ok::<(), frogdb_vll::ContinuationError>(())` (line 37) — a type annotation, renamed |
| `.scratch/hardening/specs/vll-failure-modes.md` | 109 | Preamble mapping-location sentence (15–18) and generic-string note (26–30); `FM-VLL-001`–`004` `Observable` / `Outcome variant` fields (44, 47, 56, 59, 68, 71, 90, 93) |
| `website/src/content/docs/architecture/vll.md` | — | Pins the generic reply at 167–170 and 237–239 |
| `website/src/content/docs/architecture/glossary.md` | — | Line 144 pins the `-BUSY` string — already correct, already describing the fixed behavior |

Not edited: `frogdb-txn` (neither error type is referenced from it, verified by
grep — the area gate command covers both crates but only `frogdb-vll` changes).

## Problem

### The two enums are the same enum

Verbatim, coordinator.rs:74–87 and 148–153:

```rust
pub enum ScatterError {
    ShardUnavailable(ShardSinkError),
    LockFailed { shard_id: usize, error: VllError },
    LockChannelClosed { shard_id: usize },
    LockTimeout { shard_id: usize },
    ResultChannelClosed { shard_id: usize },   // execution half
    ResultTimeout { shard_id: usize },         // execution half
}

pub enum ContinuationError {
    ShardUnavailable(ShardSinkError),
    LockFailed { shard_id: usize, error: VllError },
    LockChannelClosed { shard_id: usize },
    LockTimeout { shard_id: usize },
}
```

`ContinuationError` is `ScatterError` minus the two result-phase variants — not a
different failure model, just a different *phase count*. Both are produced by the
same three-way `match` on `tokio::time::timeout(_, ready_rx)`: compare
coordinator.rs:247–264 with coordinator.rs:394–409. The arms are line-for-line
parallel; only the constructor name differs.

Under the **deletion test**: delete `ContinuationError` and no complexity
reappears anywhere — the continuation path needs exactly the four states scatter
already names. It is a duplicate, not a distinction.

### Both mappers, in full

`scatter_error_to_response` (executor.rs:141–193) — 6 variants, 7 replies, every
arm logs `conn_id` + `txid` + `shard_id`:

| Arm | Lines | Reply |
|---|---|---|
| `ShardUnavailable(e)` | 143–152 | `ERR shard unavailable` |
| `LockFailed` where `error == ShardBusy` | 153–157 | `BUSY shard busy with continuation lock; retry` |
| `LockFailed` otherwise | 153, 157–159 | `ERR VLL lock acquisition failed` |
| `LockChannelClosed` | 161–167 | `ERR VLL lock acquisition failed` |
| `LockTimeout` | 168–174 | `ERR VLL lock acquisition failed` |
| `ResultChannelClosed` | 175–181 | `ERR shard dropped VLL result` |
| `ResultTimeout` | 182–191 | `ERR VLL execution timeout` |

`continuation_error_to_response` (eval.rs:268–281) — 4 variants, **no logging**:

| Arm | Lines | Reply |
|---|---|---|
| `ShardUnavailable(_)` | 270 | `ERR shard unavailable` |
| `LockFailed { error, .. }` | 271–273 | `ERR lock acquisition failed: {error}` (`VllError`'s `Display`, types.rs:40–50) |
| `LockChannelClosed { .. }` | 274–276 | `ERR shard dropped lock request` |
| `LockTimeout { shard_id }` | 277–279 | `ERR lock acquisition timeout on shard {shard_id}` |

### The asymmetry, condition by condition

| Condition at the shard / coordinator | Variant | Scatter reply (executor.rs) | Continuation reply (eval.rs) | Agree? |
|---|---|---|---|---|
| Sink send failed (channel closed) | `ShardUnavailable` | `ERR shard unavailable` | `ERR shard unavailable` | yes |
| Shard replied `Failed(ShardBusy)` | `LockFailed` | `BUSY shard busy with continuation lock; retry` | `ERR lock acquisition failed: Shard busy with continuation lock` | **no — retryable vs fatal** |
| Shard replied `Failed(LockTimeout)` | `LockFailed` | `ERR VLL lock acquisition failed` | `ERR lock acquisition failed: VLL lock acquisition timeout` | no |
| Shard replied `Failed(QueueFull)` | `LockFailed` | `ERR VLL lock acquisition failed` | `ERR lock acquisition failed: VLL queue full` | no |
| Shard replied `Failed(Aborted)` / `Failed(Internal(m))` | `LockFailed` | `ERR VLL lock acquisition failed` | `ERR lock acquisition failed: VLL operation aborted` / `…: VLL internal error: {m}` | no |
| `ready_tx` dropped before a reply | `LockChannelClosed` | `ERR VLL lock acquisition failed` | `ERR shard dropped lock request` | no |
| Coordinator-side wait for `Ready` expired | `LockTimeout` | `ERR VLL lock acquisition failed` | `ERR lock acquisition timeout on shard {shard_id}` | no — **and leaks an internal shard id to the client** |
| Result channel dropped | `ResultChannelClosed` | `ERR shard dropped VLL result` | n/a | — |
| Gather wait expired | `ResultTimeout` | `ERR VLL execution timeout` | n/a | — |

One of seven agrees. Six differ, and each difference is an accident of which file
the author was editing — there is no place in the tree where the difference is
stated, justified, or tested.

Two secondary observations fall out of the table:

- **The continuation path is silent.** Every scatter failure emits a `warn!` with
  `conn_id`, `txid`, and `shard_id`; a cross-shard `EVAL` that fails to acquire
  produces no server-side record whatsoever. (Adjacent to the lane's metrics item
  — routed, not claimed, below.)
- **The continuation path leaks topology.** `ERR lock acquisition timeout on shard
  {shard_id}` puts an internal shard index on the wire. Scatter deliberately logs
  the shard id and does not send it. That asymmetry argues the scatter side is the
  better baseline for everything except the missing detail.

### The live bug

`VllError::ShardBusy` means "this shard is exclusively held by someone else's
continuation lock, right now" — the most transient condition VLL produces, and the
one the shard is guaranteed to clear (`FM-VLL-003`: a parked continuation is
granted at the drain point or fails at its 2 s deadline; `FM-VLL-002`/`004`: the
holder releases on the guard's drop, coordinator.rs:138–144). The `-BUSY` code
exists to tell a client exactly that. A client that retries on `-BUSY` and
surfaces `-ERR` to the application — the standard split — gets correct behavior
for cross-shard `MGET` and a spurious hard failure for a cross-shard `EVAL` that
happened to race a Lua script on the same shard.

This is not theoretical drift: `FM-VLL-002`'s `Observable` field
(vll-failure-modes.md:56) *pins* the fatal string as the specified behavior, so
the spec currently ratifies the bug. That is what makes this spec-first.

### Nothing tests either mapper

Campaign-2 issue 10 (`.scratch/hardening-2/issues/open/10-scatter-error-replies-have-no-tests.md`)
measured it: `ERR VLL lock acquisition failed` and `ERR shard dropped VLL result`
occur **zero times outside `executor.rs`**. This proposal's own grep reproduces
that and extends it — `ERR lock acquisition failed:`, `ERR shard dropped lock
request`, and `ERR lock acquisition timeout on shard` likewise occur nowhere but
`eval.rs:268–281`. Round-2 issue 88 (line 219) records both `Display` impls and
`VllError`'s as `untested` (tests = 0).

The reason is structural, and it is a **seam** problem: `scatter_error_to_response`
is a private method on `ScatterGatherExecutor`, so reaching it needs
`shard_senders`, a `MetricsRecorder`, a `conn_id`, and (under `turmoil`) a
`ChaosConfig`; `continuation_error_to_response` is a private free function in a
server module whose only caller sits inside a closure passed to the coordinator by
a live `ConnectionHandler`. The reply table is a pure function of one enum, and it
is reachable only through the two heaviest objects in the crate.

### The gate cannot see it

`frogdb-vll` carries a 0.90 mutation gate (CLAUDE.md; ADR-0002 records 100% at the
Phase 1 lock). `cargo mutants -p <crate>` runs only that package's own tests, and
generates mutants only in that package. Both string tables live in
`frogdb-server`, which has no gate. So the mutant that rewrites executor.rs:156
from `"BUSY shard busy with continuation lock; retry"` to `""` — or to `"ERR …"` —
is never generated, and if it were, no test would kill it. `FM-VLL-001` and
`FM-VLL-004` both say "the client sees `-BUSY …`", their forcing tests
(`sca_lock_request_rejected_while_continuation_held` shard.rs:597,
`sca_lock_request_refused_while_a_continuation_request_is_parked` shard.rs:858)
assert `ShardReadyResult::Failed(ShardBusy)` at the shard layer, and the sentence
about the client is forced by nothing.

## Proposed change

### The type

One acquisition error, in `frogdb-vll`:

```rust
/// Failure of the acquisition half of any VLL choreography — scatter or
/// continuation. Both entry paths reach the same four states, so they answer
/// the client with the same reply, chosen here and nowhere else.
#[derive(Debug)]
pub enum VllAcquireError {
    ShardUnavailable(ShardSinkError),
    LockFailed { shard_id: usize, error: VllError },
    LockChannelClosed { shard_id: usize },
    LockTimeout { shard_id: usize },
}

impl VllAcquireError {
    /// The client-visible reply. `ShardBusy` is the only retryable condition
    /// VLL acquisition produces, and is the only one answered with `-BUSY`.
    pub fn to_response(&self) -> Response { … }

    /// The participant the failure is attributed to — for the host's log line.
    pub fn shard_id(&self) -> usize { … }
}

/// Scatter adds the execute/gather half on top of acquisition.
#[derive(Debug)]
pub enum ScatterError {
    Acquire(VllAcquireError),
    ResultChannelClosed { shard_id: usize },
    ResultTimeout { shard_id: usize },
}

impl ScatterError {
    pub fn to_response(&self) -> Response { … }   // delegates for `Acquire`
}
```

`ContinuationError` is deleted; `acquire_continuation_and_run` returns
`Result<T, VllAcquireError>`. Both `Display` impls collapse to one plus a short
wrapper. Call sites shrink to:

```rust
// executor.rs
Err(err) => { self.log_scatter_error(&err, txid); return err.to_response(); }

// eval.rs:165-168
match outcome { Ok(resp) => resp, Err(err) => err.to_response() }
```

and `continuation_error_to_response` (eval.rs:268–281) is deleted outright.
Logging stays host-side: `conn_id`, `txid`, and `self.timeout` are host facts and
have no business in the locking crate.

### Where the mapping lives — and ADR-0002

ADR-0002 governs the `TxnHost` seam in `frogdb-txn` ("when in doubt, plain-data
signatures through the trait"). Neither `ScatterError` nor `ContinuationError`
crosses `TxnHost` — verified, `frogdb-txn` references neither — so the rule does
not bind here. What does apply is the ADR's underlying reason for the extraction:
put the behavior where the gate that guards it can see it.

`frogdb-vll/Cargo.toml:12` already declares `frogdb-protocol`, and the crate
currently uses it nowhere (zero `frogdb_protocol` references in `src/`). So
`to_response() -> frogdb_protocol::Response` needs **no manifest change** and
introduces no new dependency edge; it also puts the crate's declared dependency to
work for the first time.

*Alternative if the reviewer prefers plain data:* `fn wire_reply(&self) ->
(&'static str /* code */, Option<String> /* detail */)` with a three-line renderer
in each host. It keeps `frogdb-vll` protocol-free but re-splits the reply decision
across two files, which is the exact drift this proposal closes — and the two
renderers would be free to disagree again. **Recommend `to_response`; flag the
seam choice for sign-off rather than assuming it.**

### The new pinned string set

Rule: the retryable condition keeps its distinct code; everything else keeps the
`ERR VLL lock acquisition failed` prefix the docs already name, gains the reason
suffix the continuation path already provides, and never carries a shard id.

| Variant | Unified reply |
|---|---|
| `ShardUnavailable` | `ERR shard unavailable` *(both paths already; unchanged)* |
| `LockFailed { error: ShardBusy }` | `BUSY shard busy with continuation lock; retry` *(scatter unchanged; **continuation changes**)* |
| `LockFailed { error: e }` (`QueueFull` / `LockTimeout` / `Aborted` / `Internal`) | `ERR VLL lock acquisition failed: {e}` |
| `LockChannelClosed` | `ERR VLL lock acquisition failed: shard dropped the lock request` |
| `LockTimeout` | `ERR VLL lock acquisition failed: coordinator timeout` |
| `ResultChannelClosed` *(scatter only)* | `ERR shard dropped VLL result` *(unchanged)* |
| `ResultTimeout` *(scatter only)* | `ERR VLL execution timeout` *(unchanged)* |

**Deliberately not folded in:** whether `QueueFull` should also be retryable
(`-BUSY`). It is arguably as transient as `ShardBusy`, but no FM row covers it,
the website documents it as `ERR` (vll.md:237–239), and changing it is a separate
client-contract decision. Left as a stated open question so the unification does
not smuggle a second behavior change.

### Spec-first sequence

**Step 1 — edit the rows.** No code yet.

| Row / location | Field | Today | After |
|---|---|---|---|
| Preamble (15–18) | mapping location | "lives in `…/scatter/executor.rs` (`scatter_error_to_response`) and `…/scripting/eval.rs` (`continuation_error_to_response`)" | "lives in `VllAcquireError::to_response` (`frogdb-vll`); the hosts only log" |
| Preamble (26–30) | generic string | `-ERR VLL lock acquisition failed` | same prefix, `: {reason}` suffix |
| FM-VLL-001 (44) | Observable | `-BUSY shard busy with continuation lock; retry` | **unchanged** |
| FM-VLL-001 (47) | Outcome variant | `→ ScatterError::LockFailed` | `→ VllAcquireError::LockFailed` |
| FM-VLL-002 (56) | Observable | `-ERR lock acquisition failed: Shard busy with continuation lock` | `-BUSY shard busy with continuation lock; retry` |
| FM-VLL-002 (57) | NOT observable | — | add: *the same shard-busy condition answered with a fatal code on one entry path and a retryable one on another — the reply is a function of the error, not of the caller* |
| FM-VLL-002 (59) | Outcome variant | `→ ContinuationError::LockFailed` | `→ VllAcquireError::LockFailed` |
| FM-VLL-003 (68) | Observable | `-ERR lock acquisition failed: VLL lock acquisition timeout` | `-ERR VLL lock acquisition failed: VLL lock acquisition timeout` |
| FM-VLL-003 (71) | Outcome variant | `→ ContinuationError::LockFailed` | `→ VllAcquireError::LockFailed` |
| FM-VLL-004 (90) | Observable | refers to FM-VLL-001 | **unchanged** |
| FM-VLL-004 (93) | Outcome variant | `→ ScatterError::LockFailed` | `→ VllAcquireError::LockFailed` |

**Step 2 — write the failing tests.** In `frogdb-vll`, tagged and appended to the
four rows' `Forced by` lists:

- A table test over `VllAcquireError::to_response`: 4 variants × the 5 `VllError`
  values on `LockFailed`, asserting exact reply bytes. Tagged `// FM-VLL-001`,
  `// FM-VLL-002`, `// FM-VLL-003`, `// FM-VLL-004` on the cases that force each
  row's `Observable`.
- An end-to-end case driving a client into the busy arm through **both** entry
  paths and asserting `BUSY shard busy with continuation lock; retry` off the wire
  — the thing issue 10 says no test does. The `EVAL` half of this **fails today**;
  that failure is the bug, made visible before it is fixed.

**Step 3 — unify.** Introduce `VllAcquireError`, fold `ContinuationError` into it,
move both string tables into `to_response`, delete
`continuation_error_to_response`, reduce the two host arms to log + delegate.

**Step 4 — docs.** `vll.md:167–170` and `237–239` (suffix added to the generic
reply); `glossary.md:144` needs no change — it already describes the fixed
behavior.

**Step 5 — re-gate.** `just mutants-diff frogdb-vll`, then `just mutants
frogdb-vll` + `just mutants-gate frogdb-vll 0.90`. `just lint-failure-modes` must
pass in both directions.

## Testability improvement

**What is untestable today.** The reply table — the only part of VLL a client
actually keys behavior off — is a pure function of one enum that cannot be called
without constructing either a `ScatterGatherExecutor` (shard senders, metrics
recorder, conn id, chaos config) or a live `ConnectionHandler` plus a coordinator
plus real shards. Consequently: zero tests, per issue 10 and issue 88.

**What becomes testable.** `VllAcquireError::to_response` is a `&self -> Response`
on a public type in a crate whose whole suite runs in under a second. Every row's
`Observable` sentence becomes an assertion:

- **The retry contract as an invariant, not a coincidence.** One test asserts that
  `ShardBusy` — and only `ShardBusy` — yields a reply beginning `BUSY`. Add a
  `VllError` variant and the wildcard-free match makes it a compile error until
  someone decides its retryability, the same forcing function `MockTxnHost` gives
  `TransactionOutcome` under ADR-0002.
- **Entry-path independence.** A single test can assert
  `scatter_err.to_response() == continuation_err.to_response()` for the same
  underlying `VllAcquireError`. Today that property is unstatable — there are two
  types, so there is nothing to compare.
- **No topology on the wire.** Assert no reply contains a shard index. Currently
  violated by eval.rs:278 and pinned by nothing.
- **`FM-VLL-001`–`004`'s client sentences get real witnesses**, closing the
  campaign-2 H7 heuristic instance issue 10 calls the largest client-facing blast
  radius.

**Mutation reachability — the real win.** Today `cargo mutants -p frogdb-vll`
generates no mutant of either string table, because neither is in the crate. After
the move, the `ShardBusy` arm, the prefix, and every reply constant are inside the
0.90 gate, and the table test kills their mutants. The gate stops attesting to a
contract it cannot see.

## Risks / scope boundaries vs siblings

**Client-visible behavior change (intended).** Cross-shard `EVAL`/`EVALSHA`
racing a continuation lock now answers `-BUSY` instead of `-ERR`. That is the fix.
FrogDB is pre-production and CLAUDE.md permits breaking changes that are
improvements; this one makes the code match its own published glossary.

**String churn is test-free.** Verified by grep: `ERR VLL lock acquisition
failed`, `ERR shard dropped VLL result`, `ERR lock acquisition failed:`, `ERR
shard dropped lock request`, and `ERR lock acquisition timeout on shard` appear in
**no test, no tcl regression file, and no fixture** — only in the two mappers
themselves, the spec, the website, and scratch notes. Adding the `: {reason}`
suffix therefore breaks no assertion; a client matching the existing prefix is
also unaffected. Only exact-equality matchers on the bare generic string would
notice, and none exist in-tree.

**A third spelling exists and is explicitly out of scope.**
`ERR shard busy with continuation lock` — no `BUSY` code, no `; retry` — is
produced by `ShardWorker::can_execute_during_lock`
(`core/src/shard/worker.rs:859`) with a fallback at
`core/src/shard/dispatch_core.rs:202`, and asserted in `search/merge.rs`
(887/893, 950/955, 976/981) and `scatter/broadcast.rs` (1057/1067). That is a
**different seam and a different condition**: the shard-local admission gate
refusing a *foreign connection's already-dispatched command*, not a lock
*acquisition*. It is pinned by the C3 seam lint
(`scripts/continuation-lock-gate.py`, chokepoint `can_execute_during_lock`) and by
those tests. Do not sweep it into this change. Whether it should also carry `-BUSY`
is a real question — file it separately; touching it here would put a seam-linted
chokepoint and six live assertions into a diff that is otherwise test-free.

**Blast radius is smaller than the spec implies.** The spec preamble (line 11) and
`lib.rs:9` both describe the continuation lock as what "a cross-shard Lua script /
MULTI" takes. In fact `acquire_continuation_and_run` has exactly two callers:
`eval.rs:134` and `shard-harness/tests/scenario_s4.rs:29`. No `frogdb-txn` or
`EXEC` path acquires a continuation lock today. So `FM-VLL-002`/`003`'s
client-visible strings are reachable only via `EVAL`/`EVALSHA` — which bounds the
change, and is itself worth a spec-prose correction (see discrepancies).

**Mutation re-gate required.** `frogdb-vll` is LOCKED at 0.90 and this change adds
branching logic to it. `just mutants-diff frogdb-vll` as push discipline; full run
`just mutants frogdb-vll` + `just mutants-gate frogdb-vll 0.90`. Expect the score
to *move* — new mutable code lands in the gated crate and the table test exists to
kill it. `frogdb-txn` is untouched (it shares the `txn` area in `just core-test`
but has no code in this diff).

**Sibling boundaries — one shared code file, one shared spec file.**

- **Proposal 45 (`vll-key-ownership-diagnostics`, lane items 1 + 13)** owns
  `vll/src/shard.rs`, `vll/src/lock_table.rs`, and `core/src/shard/vll.rs`. No code
  overlap: 46 touches only `coordinator.rs` in that crate. **Spec overlap is real**
  — 45 edits `FM-VLL-003`'s NOT-observable and `FM-VLL-005`'s Invariant, 46 edits
  `FM-VLL-003`'s Observable and Outcome variant. Different fields, adjacent lines.
  46's spec diff is the smaller one; land 46 first and rebase 45 onto it.
- **Proposal 47 (`eval-orchestration-dedup`, lane item 4)** owns `eval.rs`
  `handle_eval` (19–63), `handle_evalsha` (172–216), `ScriptShards`, and `EvalKind`.
  **Same file as 46, disjoint regions**: 46 owns *only* the `Err(err) =>` arm at
  165–168 and the function at 268–281. Neither may touch the other's region. If 47
  also takes lane item 5 (the `ERR shard dropped request` / `ERR script execution
  failed` split at eval.rs:111 and 159), that lives inside 47's region — but both
  proposals then change reply strings in one file, so land **46 → 47** and 47 must
  not re-open the acquisition mapping.
- **Proposal 52 (`vll-unknown-txid-refusal`, lane item 14)** owns
  `core/src/shard/vll.rs::handle_vll_execute` and appends a new `FM-VLL-006`. No
  code overlap; the spec edit is append-only, so it merges in either order.
- **Lane item 3 (continuation acquisition unobservable)** is the one genuine
  collision: it edits `coordinator.rs::acquire_continuation` (a `finish(outcome)`
  metrics hoist) and `eval.rs:131` (`NoopMetricsSink` → a real sink) — both files
  46 edits, and `acquire_continuation` is the function whose error type 46
  changes. 46 does **not** claim it, but sequencing 46 first makes item 3's diff
  mechanical. Note that 46's problem statement surfaces the same gap from the
  logging side (the continuation path emits no `warn!` at all); whoever takes item
  3 should take the missing `warn!` with it.

## Effort

**M.** The code is S: one enum, one `to_response`, one wrapper enum, two call
sites reduced to log-plus-delegate, one deleted free function, one test-file type
annotation. What makes it M is the locked-crate tax — four spec rows and two
preamble sentences edited, a table test plus an end-to-end two-entry-path test
written *before* the fix, `Forced by` lists extended, `just lint-failure-modes`
re-run, and the `frogdb-vll` 0.90 gate re-run. Plus two website lines. No manifest
changes, no dependency changes, no caller churn outside the two mappers.

### Independently-landable hotfix

**Add the missing `ShardBusy` arm to `continuation_error_to_response` (S).** This
is the entire live-bug content of the proposal and needs none of the refactor.
Exact diff shape, at eval.rs:271–273:

```rust
        ContinuationError::LockFailed { error, .. } => {
            if matches!(error, frogdb_vll::VllError::ShardBusy) {
                Response::error("BUSY shard busy with continuation lock; retry")
            } else {
                Response::error(format!("ERR lock acquisition failed: {error}"))
            }
        }
```

Four lines, mirroring executor.rs:155–159 exactly.

Two things to be explicit about:

1. **It is still spec-first.** It changes `FM-VLL-002`'s pinned `Observable`
   (vll-failure-modes.md:56), so the row edit and a failing forcing test land
   first, in that order. It does **not** touch `FM-VLL-003` (that row's string
   changes only when the prefix is unified) and does not touch the preamble.
2. **It needs no `frogdb-vll` re-gate.** The diff is entirely in `frogdb-server`,
   which carries no mutation gate, so `just mutants` is not on the critical path —
   but that is also exactly why the fix is fragile on its own: nothing stops the
   arm being deleted again. The forcing test should therefore be written so it can
   **move into `frogdb-vll` unchanged** when the unification lands (assert on the
   reply bytes for a given `VllError`, not on a `ConnectionHandler`), because a
   test that stays in `frogdb-server` contributes nothing to the 0.90 score.

Land the hotfix whether or not the unification is approved. The unification's
remaining value is then structural: it converts "two mappers that happen to agree"
into one mapper that cannot disagree, and moves it under the gate that is supposed
to be guarding it.
