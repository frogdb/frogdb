---
title: Configuration Reference
description: Complete reference for all FrogDB configuration options
---

import ConfigTable from '../../../components/ConfigTable.astro';
import ConfigDefault from '../../../components/ConfigDefault.astro';
import { LinkCard, CardGrid } from '@astrojs/starlight/components';

FrogDB is configured with a TOML file. Every table on this page is extracted directly from the `Config` struct in `frogdb-server/crates/config/src/`, so its parameter names, types, and defaults match the binary. For the four config surfaces (file, environment variables, CLI flags, `CONFIG SET`) and their precedence, see [Operations → Configuration](/operations/configuration/); this page does not repeat that model.

**Legend:** The **Runtime** column shows whether a field can be changed at runtime via `CONFIG SET`:
- **✓** — mutable at runtime
- **✗** — immutable (requires restart)
- **—** — not exposed via `CONFIG GET`/`CONFIG SET`

Where a field's `CONFIG` parameter name differs from its TOML key, it is shown below the TOML key as `CONFIG: <name>`.

## Configuration file format

Pass a TOML file's path with `--config`:

```bash
frogdb-server --config /etc/frogdb/frogdb.toml
```

```toml
# frogdb.toml
[section]
key = value
```

Every field below also accepts a `FROGDB_`-prefixed environment variable and, for a subset of fields, a CLI flag; see [Operations → Configuration](/operations/configuration/) for the exact mapping and precedence rules.

---

## Server

Network and connection settings.

```toml
[server]
```

<ConfigTable section="server" />

Example:

```toml
[server]
bind = "127.0.0.1"
port = 6379
num-shards = 0          # auto-detect CPU cores
max-clients = 10000
```

## Persistence

Data durability settings backed by RocksDB.

```toml
[persistence]
```

<ConfigTable section="persistence" />

Example:

```toml
[persistence]
enabled = true
data-dir = "/var/lib/frogdb"
durability-mode = "sync"
compression = "lz4"
block-cache-size-mb = 512
```

### Durability modes

| Mode | Description |
|------|-------------|
| `async` | Writes are buffered and flushed asynchronously. Fastest, but data can be lost on crash. |
| `periodic` | Writes are synced at a configurable interval (`sync-interval-ms`). Good balance of performance and safety. |
| `sync` | Every write is synced immediately. Safest, but highest latency. |

## Snapshot

Point-in-time snapshot settings.

```toml
[snapshot]
```

<ConfigTable section="snapshot" />

## Memory

Memory management and eviction settings.

```toml
[memory]
```

<ConfigTable section="memory" />

### Eviction policies

| Policy | Description |
|--------|-------------|
| `noeviction` | Return an error when the memory limit is reached. |
| `allkeys-lru` | Evict least recently used keys. |
| `allkeys-lfu` | Evict least frequently used keys. |
| `allkeys-random` | Evict random keys. |
| `volatile-lru` | Evict LRU keys with a TTL set. |
| `volatile-lfu` | Evict LFU keys with a TTL set. |
| `volatile-random` | Evict random keys with a TTL set. |
| `volatile-ttl` | Evict keys with the shortest TTL. |
| `tiered-lru` | Spill LRU keys to the warm tier instead of deleting them (requires `tiered-storage.enabled`). |
| `tiered-lfu` | Spill LFU keys to the warm tier instead of deleting them (requires `tiered-storage.enabled`). |

## Replication

Primary-replica replication settings.

```toml
[replication]
```

<ConfigTable section="replication" />

Example — configuring a replica:

```toml
[replication]
role = "replica"
primary-host = "primary.example.com"
primary-port = 6379
```

Example — configuring a primary with a write quorum:

```toml
[replication]
role = "primary"
min-replicas-to-write = 1
min-replicas-timeout-ms = 5000
```

## Cluster

Cluster mode settings using Raft consensus.

```toml
[cluster]
```

<ConfigTable section="cluster" />

Example:

```toml
[cluster]
enabled = true
cluster-bus-addr = "10.0.1.1:16379"
initial-nodes = ["10.0.1.2:16379", "10.0.1.3:16379"]
auto-failover = true
```

## HTTP

Unified HTTP endpoint (default port <ConfigDefault path="http.port" />) serving Prometheus metrics at `/metrics` and the admin/debug API at `/admin/*` and `/debug/*`.

```toml
[http]
```

<ConfigTable section="http" />

## Admin

RESP-protocol listener for administrative commands, separate from the main client port.

```toml
[admin]
```

<ConfigTable section="admin" />

## TLS

TLS settings for the RESP, admin, HTTP, replication, and cluster-bus listeners.

```toml
[tls]
```

<ConfigTable section="tls" />

## Security

Authentication settings.

```toml
[security]
```

<ConfigTable section="security" />

For fine-grained access control, see ACL below.

## ACL

Access Control List settings.

```toml
[acl]
```

<ConfigTable section="acl" />

## Logging

Logging configuration.

```toml
[logging]
```

<ConfigTable section="logging" />

## Metrics

Prometheus metrics and OTLP export settings.

```toml
[metrics]
```

<ConfigTable section="metrics" />

Example:

```toml
[metrics]
otlp-enabled = true
otlp-endpoint = "http://otel-collector:4317"
otlp-interval-secs = 30
```

## Slow Log

Slow query logging.

```toml
[slowlog]
```

<ConfigTable section="slowlog" />

## Blocking

Limits for blocking commands (`BLPOP`, `BRPOP`, `BLMOVE`, etc.).

```toml
[blocking]
```

<ConfigTable section="blocking" />

## JSON

Limits for the JSON data type (`JSON.SET`, `JSON.GET`, etc.).

```toml
[json]
```

<ConfigTable section="json" />

## Tiered Storage

Two-tier hot/warm storage that spills values to RocksDB instead of evicting them.

```toml
[tiered-storage]
```

<ConfigTable section="tiered-storage" />

Tiered storage requires `persistence.enabled = true` and a `tiered-lru` or `tiered-lfu` eviction policy.

## Distributed Tracing

OpenTelemetry-compatible distributed tracing.

```toml
[tracing]
```

<ConfigTable section="tracing" />

## Compatibility

Redis protocol compatibility behavior.

```toml
[compat]
```

<ConfigTable section="compat" />

## Status

Health endpoint thresholds used by the status API.

```toml
[status]
```

<ConfigTable section="status" />

## Latency Testing

Startup latency testing and monitoring.

```toml
[latency]
```

<ConfigTable section="latency" />

## Latency Bands

SLO-focused latency bucket tracking.

```toml
[latency-bands]
```

<ConfigTable section="latency-bands" />

## Debug Bundle

Diagnostic bundle generation for troubleshooting.

```toml
[debug-bundle]
```

<ConfigTable section="debug-bundle" />

## MONITOR

Settings for the `MONITOR` command.

```toml
[monitor]
```

<ConfigTable section="monitor" />

## See also

<CardGrid>
  <LinkCard title="Configuration surfaces" description="File, env vars, CLI flags, and CONFIG SET precedence" href="/operations/configuration/" />
  <LinkCard title="Example config file" description="A complete, generated config file" href="/reference/example-config/" />
  <LinkCard title="frogdb-server" description="Server command-line options" href="/reference/frogdb-server/" />
  <LinkCard title="frogctl" description="Operational CLI tool" href="/reference/frogctl/" />
</CardGrid>
