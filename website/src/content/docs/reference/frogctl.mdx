---
title: frogctl
description: Command-line reference for the FrogDB operational CLI
---

import CliOptionsTable from '../../../components/CliOptionsTable.astro';
import CliCommandsTable from '../../../components/CliCommandsTable.astro';
import { LinkCard, CardGrid } from '@astrojs/starlight/components';

Operational CLI tool for managing FrogDB.

```bash
frogctl [GLOBAL OPTIONS] <COMMAND>
```

## Global options

Generated from `GlobalOpts` in `frogctl/src/cli.rs`.

<CliOptionsTable source="frogctl-cli" />

## Commands

Generated from the `Commands` enum in `frogctl/src/cli.rs`. This is the top-level command list; run `frogctl <command> --help` for a subcommand's own flags.

<CliCommandsTable source="frogctl-cli" />

## Examples

```bash
# Health check
frogctl health

# Real-time stats dashboard
frogctl stat

# Execute a raw command
frogctl exec GET mykey

# Scan keys matching a pattern
frogctl scan --match "user:*"

# Watch live commands
frogctl watch

# Cluster info
frogctl cluster info

# Backup
frogctl backup snapshot

# Output as JSON
frogctl -o json health

# Connect with TLS
frogctl --tls --tls-ca ca.crt health

# Benchmark
frogctl benchmark
```

## See also

<CardGrid>
  <LinkCard title="frogdb-server" description="Server command-line options" href="/reference/frogdb-server/" />
  <LinkCard title="Command matrix" description="Redis command support status" href="/compatibility/command-matrix/" />
  <LinkCard title="Configuration Reference" description="All configuration options" href="/reference/configuration/" />
</CardGrid>
