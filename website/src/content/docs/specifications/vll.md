---
title: "VLL (multi-shard locking) — failure modes"
description: "Very Lightweight Locking: acquisition, continuation, and drain."
sidebar:
  order: 5
---
<!-- DO NOT EDIT — auto-generated by: just spec-gen (source: specs/vll.md) -->

| | |
|---|---|
| **Status** | LOCKED (2026-08-01) |
| **Gate** | 0.90 |
| **Crates** | frogdb-vll |

Phase 1 mutation gate passed (frogdb-vll 100%). Behavior changes to this area are spec-first;
see CLAUDE.md "Locked core areas".

Every way the shard-exclusive half of VLL can refuse a request, one table per mode. Same
contract as [Transactions — failure modes](/specifications/txn/): a mutant that survives is a
row nothing forces.

Scope: the per-shard state machine and the cross-shard coordinator in
`frogdb-server/crates/vll/src/{shard,coordinator}.rs` — specifically the **continuation lock**
(the shard-exclusive lock a cross-shard Lua script takes; a cross-shard `MULTI` never reaches it —
see [txn.md TR-TXN-019](/specifications/txn/#tr-txn-019--cross-shard-multi-is-refused-at-exec)) and its
interaction with the SCA scatter queue. Rows stop at what the shard reports over `ShardReadyResult`; the mapping to
the client's reply is named in `Observable` and lives in
`frogdb-server/crates/server/src/scatter/executor.rs` (`scatter_error_to_response`) and
`frogdb-server/crates/server/src/connection/scripting/eval.rs`
(`continuation_error_to_response`).

One caller-side obligation is in scope even though it lives outside those two files: the
shard worker must pair every `dequeue_for_execution` with a `release_after_execution`
(`frogdb-server/crates/core/src/shard/vll.rs`). The lock table cannot enforce this from
the inside — a caller that never returns leaves the entry held forever — so the way that
pairing survives an abnormal exit is rowed here ([FM-VLL-005](#fm-vll-005--a-granted-op-panics-while-executing)).

The scatter phases are in scope too: TR-VLL-016/017/018 row the phase-1/2/3 partial-failure
unwinds ([FM-VLL-008](#fm-vll-008--a-scatter-phase-fails-partway-through)) and TR-VLL-019 rows the
phase-4 gather timeout ([FM-VLL-009](#fm-vll-009--a-scatter-gather-wait-times-out-after-execute-was-dispatched)),
which is the one scatter failure that does not unwind — past phase 3 there is nothing left to
unwind *to*.

## State space

Variable names below are the tokens `## Transitions` pre/postconditions reference. See
[txn.md](/specifications/txn/#state-space) for why cross-shard continuation state is owned here, not there.

| Variable | Authoritative field | Writer(s) | Persistence | Survives restart |
|---|---|---|---|---|
| `lock_table.intents` | `LockTable.keys: HashMap<Bytes, BTreeMap<u64, Intent>>` (`Intent{mode, granted}`), BTreeMap ordering realizes SCA priority (lower txid = higher priority) | `LockTable::declare`/`try_grant`/`release` | shard-local, in-memory | No |
| `shard.tx_queue` | `VllShardState.tx_queue: Option<TransactionQueue<O>>` (`BTreeMap<u64, VllPendingOp<O>>`) | `enqueue_lock_request`/`dequeue_for_execution`/`abort` | shard-local, in-memory | No |
| `op.state` | `VllPendingOp.state: PendingOpState` (`Pending`/`Ready`/`Executing`/`Done`) | `try_acquire_for` (Pending→Ready), `dequeue_for_execution` (→Executing) — `Done` is assigned by no code path (dead variant), and the `Executing` write lands on an op already removed from `tx_queue` (only `txid`/`keys`/`operation` propagate into `DequeuedOp`), so `Executing` is never observable via `iter_pending_ops`; this makes `core/src/shard/diagnostics.rs:175`'s `executing_txid = Some(...)` arm unreachable | shard-local, per-op, in-memory | No |
| `op.ready_tx` | `VllPendingOp.ready_tx: Option<oneshot::Sender<ShardReadyResult>>` | set at `VllPendingOp::new`; consumed (`.take()`) by `mark_ready` | shard-local, per-op, in-memory | No |
| `op.wound_tx` | `VllPendingOp.wound_tx: Option<oneshot::Sender<VllError>>` — the SCA analogue of `shard.continuation_revoke_tx`: the shard's only way to tell a *granted* scatter op that an older transaction needs one of its keys | set at `VllPendingOp::new` (dispatched with the lock request, `traits.rs`'s `LockRequest`); **taken** by `VllPendingOp::wound`, so the notice fires at most once per op; dropped with the op by `dequeue_for_execution`/`abort`, which is what makes an executing op unwoundable | shard-local, per-op, in-memory | No |
| `shard.executing_ops` | `VllShardState.executing_ops: usize` | `+= 1` in `dequeue_for_execution`; `saturating_sub` in `release_after_execution` | shard-local, in-memory | No |
| `shard.continuation_lock` | `VllShardState.continuation_lock: Option<ContinuationLock>` (`{txid, conn_id, acquired_at}`) | installed by `grant_continuation`; cleared by `next_continuation_event`'s `Released` arm | shard-local, in-memory | No |
| `shard.pending_continuation_release` | `VllShardState.pending_continuation_release: Option<oneshot::Receiver<()>>` | set alongside `shard.continuation_lock` by `grant_continuation`; taken by `next_continuation_event` | shard-local, in-memory | No |
| `shard.pending_continuation` | `VllShardState.pending_continuation: Option<PendingContinuation>` (`{txid, conn_id, ready_tx, release_rx, revoke_tx, deadline}`) | set by `request_continuation_lock` when not drained *or* when it wounds a younger holder and parks behind it; taken by `try_grant_pending_continuation` (which now also runs from the `Released` arm) or the timeout arm of `next_continuation_event` | shard-local, in-memory | No |
| `shard.continuation_revoke_tx` | `VllShardState.continuation_revoke_tx: Option<oneshot::Sender<VllError>>` — the shard's only way to make a holder let go | installed with the lock by `grant_continuation`; **taken** by `revoke_continuation` so the notice fires at most once per grant; cleared with the lock by the `Released` arm | shard-local, in-memory | No |
| `shard.continuation_hold_deadline` | `VllShardState.continuation_hold_deadline: Option<Instant>` = grant time + `CONTINUATION_MAX_HOLD` (5000ms) | set by `grant_continuation`; disarmed (`None`) by `next_continuation_event` once the cap has fired, so a revoked-but-unreleased lock does not spin the host loop | shard-local, in-memory | No |
| `pending_continuation.deadline` | `PendingContinuation.deadline: Instant` = park time + `CONTINUATION_DRAIN_TIMEOUT` (2000ms) | set at park time in `request_continuation_lock` | shard-local, in-memory | No |
| `guard.release_txs` | `ContinuationGuard.release_txs: Vec<oneshot::Sender<()>>` (one per participating shard) | populated by `acquire_continuation`; drained (fired) by `Drop for ContinuationGuard` **on the success path only** — on an *acquisition-failure* unwind (`coordinator.rs:386/398/402/406`) no `ContinuationGuard` exists yet (it is constructed at `:412`, after success), so the raw `Vec<oneshot::Sender<()>>` built up so far is dropped directly, which *closes* each channel rather than sending `()`; the shard side tolerates this (`shard.rs:395`'s `let _ = release_rx.await;` accepts the `Err` and clears the lock the same way) — see TR-VLL-013 | connection-task-local (owned by the caller of `acquire_continuation_and_run`), in-memory | No — dropped with the task; see TR-VLL-021 |
| `acquisition.timeout` | caller-supplied `Duration` to `acquire_continuation`/`scatter` (default `DEFAULT_LOCK_ACQUISITION_TIMEOUT`, 4000ms). It is a **total bound on one request**, not a per-wait allowance: `scatter` converts it into a single absolute `Instant` deadline (`start + timeout`) at entry and every receiver wait in phases 2 and 4 is a `timeout_at` against that one deadline; `acquire_continuation` does the same at its own entry for its receiver loop. Participant count therefore cannot stretch the observed bound to `participants × timeout` ([FM-VLL-011](#fm-vll-011--receiver-waits-are-timed-one-at-a-time-and-sum)). Phases 2 and 4 share the one deadline rather than getting one each, so a scatter that spends nearly all of its budget acquiring keeps only the remainder for the gather — deliberate: past phase 3 the outcome is ambiguous whenever the wait ends (TR-VLL-019), and what the caller asked to be bounded is the request. `Instant` here is monotonic, request-scoped and local — timeout mechanics, not state-bearing time, so the no-wall-clock-in-state principle is not implicated. For `scatter` specifically the constant is a **lower bound**, not a default: `scatter/executor.rs:116` passes `self.timeout.max(DEFAULT_LOCK_ACQUISITION_TIMEOUT)` — only the continuation path (`eval.rs:134`) passes the constant itself | caller (`eval.rs`'s `execute_cross_shard_script`, or other scatter callers) | connection-task-local, in-memory | No |
| `txid` | `u64`, one per script invocation / cross-shard transaction, from `next_txid()` — a process-wide `AtomicU64` seeded at 1 (`frogdb-server/crates/server/src/server/util.rs:17`), `fetch_add(1, SeqCst)` at `:29` (stricter than `next_conn_id`'s `Relaxed` at `:24`; "process-wide" holds for production builds — the `AtomicU64` is the `frogdb_core::sync` seam alias, `shuttle` under that feature) | caller of `scatter`/`acquire_continuation_and_run` (e.g. `eval.rs:127`) | process-local, in-memory | No — resets to 1 on process restart, same as every other `shard.tx_queue`/`lock_table.intents` entry it orders |
| `scatter.phase_state` (ephemeral) | local `Vec` of ready/result receivers inside `VllCoordinator::scatter`'s own stack frame — not a struct field, never shared | `scatter()`'s own phases 1-4 | call-stack-local to the coordinator task, not stored anywhere | n/a |

## Transitions

Each row is the code's current behavior unless marked `Pending`, in which case the table encodes
the **ruled** target semantics (the linked issue is the authority) and the `Source` cites what
exists today. IDs are stable once assigned; do not renumber on edit.

## TR-VLL-001 — SCA lock request enqueued and granted immediately

| Field | Value |
| --- | --- |
| Precondition | shard not `continuation_held_or_pending`; `lock_table.intents` has no conflicting intent for the requested keys |
| Postcondition | `lock_table.intents` gains the declared intent; op inserted into `shard.tx_queue` with `op.state = Pending`, then advances to `Ready`; `op.ready_tx` fires `Ready` |
| Source | `frogdb-server/crates/vll/src/shard.rs:137` (`enqueue_lock_request`), `:185` (`try_acquire_for`) |
| Rulings | — |

## TR-VLL-002 — SCA lock request queues behind a conflicting txid

| Field | Value |
| --- | --- |
| Precondition | same as TR-VLL-001 but a conflicting intent exists **and the wait respects the txid order**: either an older (lower-`txid`) intent is ahead of the requester in `lock_table.intents` (SCA rule 1), or a conflicting *granted* holder is older than the requester. A conflict whose only blocker is a granted holder with a **higher** `txid` takes [TR-VLL-022](#tr-vll-022--wound-wait-resolves-an-sca-scatter-collision)'s wound path instead of parking |
| Postcondition | op stays `op.state = Pending` in `shard.tx_queue`; `op.ready_tx` not yet fired; advances only via `try_advance_pending_locks` (called from `release_after_execution`/`abort`). `try_grant` reports this as `GrantOutcome::Blocked`, and an SCA block on *any* key wins over a wound owed on another — a requester already parked behind seniority has nothing to gain from aborting juniors elsewhere |
| Source | `frogdb-server/crates/vll/src/shard.rs:137,203` (`try_advance_pending_locks`'s `fn` line — `:199` is its doc comment), `frogdb-server/crates/vll/src/lock_table.rs:90` (`try_grant`) |
| Rulings | — |

## TR-VLL-003 — SCA lock request refused: continuation lock held

| Field | Value |
| --- | --- |
| Precondition | `shard.continuation_lock ≠ None` |
| Postcondition | no intent declared, `shard.tx_queue` unchanged; `op.ready_tx` fires `Failed(ShardBusy)` immediately |
| Source | `frogdb-server/crates/vll/src/shard.rs:145` |
| Rulings | — |

## TR-VLL-004 — SCA lock request refused: continuation request parked

| Field | Value |
| --- | --- |
| Precondition | `shard.pending_continuation ≠ None` |
| Postcondition | same as TR-VLL-003 — `continuation_held_or_pending()` covers both held and parked |
| Source | `frogdb-server/crates/vll/src/shard.rs:145,329` |
| Rulings | — |

## TR-VLL-005 — Dequeue for execution

| Field | Value |
| --- | --- |
| Precondition | op has `op.state = Ready` in `shard.tx_queue` |
| Postcondition | op removed from `shard.tx_queue`; `shard.executing_ops += 1`; caller receives the dequeued op's keys/operation |
| Source | `frogdb-server/crates/vll/src/shard.rs:226` |
| Rulings | — |

## TR-VLL-006 — Release after execution

| Field | Value |
| --- | --- |
| Precondition | a `dequeue_for_execution` for this `txid` has not yet been paired with a `release_after_execution` |
| Postcondition | `shard.executing_ops -= 1` (saturating); `lock_table.intents` releases this txid's locks; `try_advance_pending_locks` runs (may move other ops Pending→Ready); `try_grant_pending_continuation` runs (may grant a parked continuation if now drained) |
| Source | `frogdb-server/crates/vll/src/shard.rs:250` |
| Rulings | — |

## TR-VLL-007 — Abort a pending or ready op

| Field | Value |
| --- | --- |
| Precondition | op present in `shard.tx_queue` (not yet dequeued for execution) |
| Postcondition | op removed from `shard.tx_queue`; `lock_table.intents` releases this txid's locks; `try_advance_pending_locks` and `try_grant_pending_continuation` both run |
| Source | `frogdb-server/crates/vll/src/shard.rs:264` |
| Rulings | — |

## TR-VLL-008 — Continuation lock granted immediately (shard already drained)

| Field | Value |
| --- | --- |
| Precondition | `continuation_held_or_pending() = false`; `is_drained() = true` (`shard.executing_ops = 0` and `shard.tx_queue` empty) |
| Postcondition | `shard.continuation_lock = Some({txid, conn_id, now})`; `shard.pending_continuation_release = Some(release_rx)`; the requester's `ready_tx` fires `Ready` — **unless** that `ready_tx` is already closed (requester gave up), in which case neither is installed and the shard stays immediately usable |
| Source | `frogdb-server/crates/vll/src/shard.rs:302` (`request_continuation_lock`), `:345` (`grant_continuation`) |
| Rulings | [issue 10](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/10-txn-vll-advisory-sweep.md) — promotes this `ready_tx`-closed guard to THE invariant of FM-VLL-003, not the 2s/4s timing margin |

## TR-VLL-009 — Continuation lock request parked (shard not drained)

| Field | Value |
| --- | --- |
| Precondition | `continuation_held_or_pending() = false`; `is_drained() = false` |
| Postcondition | `shard.pending_continuation = Some({txid, conn_id, ready_tx, release_rx, deadline: now + CONTINUATION_DRAIN_TIMEOUT})`; call is synchronous, non-blocking (never waits — it runs on the host's own event loop, the thing that drains the queue); new SCA requests refused (TR-VLL-004) until this resolves |
| Source | `frogdb-server/crates/vll/src/shard.rs:302,319` |
| Rulings | — |

## TR-VLL-010 — Parked continuation request granted from a drain point

| Field | Value |
| --- | --- |
| Precondition | `shard.pending_continuation = Some(p)`; a drain point (`release_after_execution` or `abort`) fires and `is_drained()` becomes true |
| Postcondition | `shard.pending_continuation` taken and passed to `grant_continuation` (TR-VLL-008's postcondition applies); if `p.ready_tx` was already closed, no lock installs and the shard stays immediately usable |
| Source | `frogdb-server/crates/vll/src/shard.rs:363` (`try_grant_pending_continuation`) |
| Rulings | — |

## TR-VLL-011 — Parked continuation request times out

| Field | Value |
| --- | --- |
| Precondition | `shard.pending_continuation = Some(p)`; `now ≥ pending_continuation.deadline`; the host's `next_continuation_event` select arm fires |
| Postcondition | `shard.pending_continuation = None`; `p.ready_tx` fires `Failed(LockTimeout)`; no lock installed; the drain barrier lifts (SCA requests accepted again) |
| Source | `frogdb-server/crates/vll/src/shard.rs:393` (`next_continuation_event`, `DrainTimedOut` arm) |
| Rulings | [issue 10](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/10-txn-vll-advisory-sweep.md) — ruled the shard deadline should be *derived from* `acquisition.timeout` rather than kept numerically below it, so the two cannot drift apart in a later edit; built: `CONTINUATION_DRAIN_TIMEOUT` (`shard.rs:37`) is now `DEFAULT_LOCK_ACQUISITION_TIMEOUT.as_millis() / 2` (`coordinator.rs:52`), a compile-time `const` derivation, not two independent literals |

## TR-VLL-012 — Continuation lock released

| Field | Value |
| --- | --- |
| Precondition | `shard.continuation_lock = Some(l)`; `shard.pending_continuation_release = Some(rx)`; the coordinator's `ContinuationGuard` drops (normal completion or panic in `run`), firing every entry in `guard.release_txs` |
| Postcondition | `next_continuation_event`'s `Released` arm awaits `rx`, then `shard.continuation_lock = None`, `shard.pending_continuation_release = None`; the shard accepts SCA work and new continuation requests again |
| Source | `frogdb-server/crates/vll/src/shard.rs:393` (`Released` arm), `frogdb-server/crates/vll/src/coordinator.rs:138` (`impl Drop for ContinuationGuard`) |
| Rulings | — |

## TR-VLL-013 — Second continuation-lock request refused

| Field | Value |
| --- | --- |
| Precondition | `continuation_held_or_pending() = true` (held or parked, from any connection) **and** the arriving request's `txid` is >= the existing claim's — i.e. the newcomer is the younger transaction. A *lower*-txid newcomer takes TR-VLL-014's wound path instead |
| Postcondition | the second requester's `ready_tx` fires `Failed(ShardBusy)`; `run` is never invoked for that requester; every already-acquired lock on other shards releases before the error surfaces — via the raw `Vec<oneshot::Sender<()>>` being dropped on the unwind path, which *closes* each `release_rx`; the shard's `next_continuation_event` tolerates the resulting `Err` and clears the lock the same way a normal `Released` signal would. This refusal respects the global `txid` order, so it cannot take part in a wait cycle |
| Source | `frogdb-server/crates/vll/src/shard.rs` (`request_continuation_lock`, the two `txid >= …` arms), `frogdb-server/crates/vll/src/coordinator.rs` (`acquire_continuation`, unwind-on-failure) |
| Rulings | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) — Finding H1: the *unconditional* version of this refusal, with no priority ordering, was the livelock hazard; TR-VLL-014's wound path now covers the other half |

## TR-VLL-014 — Wound-wait resolves a continuation-lock collision

| Field | Value |
| --- | --- |
| Precondition | a continuation request arrives at a shard that already has a claim (held or parked) belonging to a **higher** `txid` |
| Postcondition | the older (lower-`txid`) requester wins. A younger *holder* is sent `VllError::Wounded` on `shard.continuation_revoke_tx` and the older request parks behind it, taking the lock from the `Released` arm's drain-point grant; a younger *parked* claim is failed outright with `Failed(Wounded)` and the older request takes its slot. `shard.continuation_lock` is never cleared by the wound itself — only the holder knows when its shard-side work stopped, and it says so by dropping its guard. Coordinator-side the wound surfaces as `ContinuationError::Revoked { reason: Wounded }` (`is_wound() = true`), which the caller retries **under the same txid** (see [LV-VLL-001](#lv-vll-001--a-colliding-pair-of-continuations-always-makes-progress)). The old "shards must be sorted … to prevent deadlocks" doc comment is corrected: sorted dispatch does not buy deadlock-freedom under pipelined acquisition — the `txid` priority rule does. The same ruling governs the *other* lock this crate hands out: [TR-VLL-022](#tr-vll-022--wound-wait-resolves-an-sca-scatter-collision) applies it to SCA scatter locks, which are dispatched pipelined for the same reason and so form the same cycles |
| Source | `frogdb-server/crates/vll/src/shard.rs` (`request_continuation_lock`, `revoke_continuation`, `park_continuation`), `frogdb-server/crates/vll/src/coordinator.rs` (`RevocationWatch`, `acquire_continuation`'s revocation race, `acquire_continuation_and_run`), `frogdb-server/crates/server/src/connection/scripting/eval.rs` (`MAX_WOUND_RETRIES`, same-txid retry) |
| Rulings | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) |

## TR-VLL-015 — SCRIPT KILL, or the hold cap, revokes a held continuation lock

| Field | Value |
| --- | --- |
| Precondition | a cross-shard script holds a continuation lock (`shard.continuation_lock.conn_id` = the killed connection) via `acquire_continuation_and_run`, and either `SCRIPT KILL`/`FUNCTION KILL` reaches the shard or the lock outlives `CONTINUATION_MAX_HOLD` (5 s; FDB 5 s-cap / CRDB heartbeat-lock precedent) |
| Postcondition | the shard fires `VllError::Revoked` on `shard.continuation_revoke_tx`; `acquire_continuation_and_run` drops the `run` future and returns `ContinuationError::Revoked` (`is_wound() = false` — not retried), which drops the guard and releases the lock on **every** participating shard. The hold cap disarms after firing (`shard.continuation_hold_deadline = None`) so a revoked-but-unreleased lock does not re-fire, and the lock clears on the holder's release, not on the shard's say-so. Writes already applied on some shards before the revocation are possibly-applied, per-shard — nothing rolls them back, and the client's reply says so |
| Source | `frogdb-server/crates/vll/src/shard.rs` (`revoke_held_continuation`, `next_continuation_event`'s cap arm), `frogdb-server/crates/core/src/shard/scripting.rs` (`handle_script_kill`), `frogdb-server/crates/server/src/connection/scripting/eval.rs` (`continuation_error_to_response`) |
| Rulings | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) |

## TR-VLL-016 — Scatter phase-1 dispatch failure unwinds already-declared intents

| Field | Value |
| --- | --- |
| Precondition | `scatter` dispatching lock requests to participants in order; dispatch to participant `k` fails |
| Postcondition | every participant `0..k` (already holding/queuing an intent) is aborted; participant `k` and later never received the request and are not aborted; result is `ScatterError::ShardUnavailable` |
| Source | `frogdb-server/crates/vll/src/coordinator.rs:219-240` (Phase 1), test `phase1_dispatch_failure_aborts_shards_already_holding_intents` (`:662`) |
| Rulings | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) — Finding H3: rowed as FM-VLL-008 |

## TR-VLL-017 — Scatter phase-2 lock-acquisition failure/timeout unwinds by real shard id

| Field | Value |
| --- | --- |
| Precondition | `scatter` awaiting each dispatched participant's ready signal; one reports `Failed`, drops its channel, or is still silent when the request's single absolute deadline (`acquisition.timeout` measured from `scatter` entry, not from where the loop reached that receiver) passes |
| Postcondition | every dispatched participant (by real shard id, not vector position) is aborted; result is `ScatterError::LockFailed`/`LockChannelClosed`/`LockTimeout`. The `LockTimeout` names the participant the coordinator was waiting on when the deadline passed, and the whole of phase 2 is bounded by that one deadline — the *k*-th receiver gets the budget the first `k-1` left, never a fresh `timeout` of its own ([FM-VLL-011](#fm-vll-011--receiver-waits-are-timed-one-at-a-time-and-sum)) |
| Source | `frogdb-server/crates/vll/src/coordinator.rs:246-266` (Phase 2), tests `phase2_failure_aborts_real_shard_ids_for_sparse_participants` (`:626`), `scatter_aborts_when_shard_lock_fails` (`:595`) |
| Rulings | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) — Finding H3 |

## TR-VLL-018 — Scatter phase-3 execute-dispatch failure unwinds remaining holders only

| Field | Value |
| --- | --- |
| Precondition | `scatter` dispatching execute to each ready participant in order; dispatch to the participant at index `idx` fails |
| Postcondition | participants at `0..idx` already received execute and release their own locks on completion — not aborted; participants at `idx..` still hold granted locks with no execute in flight and are aborted by real shard id; result is `ScatterError::ShardUnavailable`. This is bounded by the same fail-stop as FM-VLL-005: a live node does not keep serving reads against a shard whose worker is gone in this window — `send_execute` (`vll_adapter.rs:117-130`) only fails with `reason: "shard channel closed"`, which an mpsc `send().await` returns solely when the receiver has been dropped, i.e. the shard worker task is gone, never merely wedged or backpressured; `shard_supervisor.rs:53-64`'s `AbortFailStop` aborts the process on exactly that event, so the partial-commit window is node-fatally bounded, not indefinitely observable |
| Source | `frogdb-server/crates/vll/src/coordinator.rs:278-290` (Phase 3), test `phase3_failure_aborts_remaining_holders_not_positions` (`coordinator.rs:710`, the `fn` line — `:709` is its `#[tokio::test]` attribute), `frogdb-server/crates/server/src/server/shard_supervisor.rs:53-64` (`AbortFailStop`) |
| Rulings | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) — Finding H3 |

## TR-VLL-019 — Scatter phase-4 gather timeout yields an AMBIGUOUS outcome

| Field | Value |
| --- | --- |
| Precondition | `scatter` awaiting a dispatched participant's execute result; the request's single absolute deadline (`acquisition.timeout` from `scatter` entry — the same deadline phase 2 spent from, so the gather gets the remainder, not a fresh allowance) passes with that shard still silent, after execute was already sent (op is executing or has executed on that shard) |
| Postcondition | the timeout does not resolve the op's outcome to commit or abort by wall clock — it yields an AMBIGUOUS outcome mirroring `TransactionOutcome`'s possibly-applied/definitely-not distinction. Phase 4 is entered only after phase-3 dispatch has already succeeded for every participant (`coordinator.rs:278-290` returns `ShardUnavailable` on the first `send_execute` failure, so `result_rxs` is either complete or the function has already exited) — so at this point every participant already received `VllExecute`, and "never ran" is reachable only via a shard worker that is gone (never merely wedged or backpressured — `send_execute` only fails when its mpsc receiver has been dropped, `vll_adapter.rs:117-130`), which is node-fatal (`process::abort()`, see FM-VLL-005 / TR-VLL-018). The op may have already applied or may still be executing; today's `ScatterError::ResultTimeout` already maps to a **distinct** client-visible error, `-ERR VLL execution timeout` (`scatter/executor.rs:182-191`), separate from the generic `-ERR VLL lock acquisition failed` that `LockFailed`/`LockChannelClosed`/`LockTimeout` share (`:158,166,173`) — and the gap this row closed was not string collision but that the old `ResultTimeout { shard_id }` named one shard and said nothing about which participants applied. `ScatterError::ResultAmbiguous { shard_id, applied, unknown }` replaces it: `applied` is the participants that answered, `unknown` the ones whose fate the timeout did not settle ([FM-VLL-009](#fm-vll-009--a-scatter-gather-wait-times-out-after-execute-was-dispatched)) |
| Source | `frogdb-server/crates/vll/src/coordinator.rs` (Phase 4), `frogdb-server/crates/server/src/scatter/executor.rs` (`ResultAmbiguous` arm) |
| Rulings | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) |

## TR-VLL-020 — A granted op panics mid-execution; locks release, read paths resume, write paths fail-stop

| Field | Value |
| --- | --- |
| Precondition | an op holds granted locks and is executing when the underlying code panics |
| Postcondition | the panic is caught at the shard boundary; TR-VLL-005/006's dequeue/release pairing stays intact across the unwind, so locks release and `shard.executing_ops` decrements correctly, and the caller sees an error-shaped result. The catch boundary is then not trusted on its own: the escalation splits on the `LockMode` the op declared. A **read**-path panic is isolated — the shard answers the next message normally. A **write**-path panic escalates to a clean process exit (code 70, distinguishable from a crash) rather than resuming on half-applied state; the node returns through ordinary startup WAL replay, which is the only restart mechanism — no shard-level restart exists. See [FM-VLL-005](#fm-vll-005--a-granted-op-panics-while-executing), whose NOT-observable set carries "partial writes surviving a panic" |
| Source | `frogdb-server/crates/vll/src/shard.rs` (dequeue/release pairing; `release_after_panic` decides the split), `frogdb-server/crates/vll/src/fail_stop.rs` (`FailStopSink`, `ProcessExitFailStop`), `frogdb-server/crates/core/src/shard/vll.rs` (`handle_vll_execute`: `panic_guard::caught` wraps `execute_scatter_part`, release runs on both the success and the panic-recovery arm) |
| Rulings | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md), [issue 28](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/28-vll-panic-restarts-the-shard-from-wal.md) — R7 re-ruled the WAL-restart-of-a-shard addendum as process fail-stop |

## TR-VLL-021 — Continuation-lock cancellation mid-run (RULED, not yet built)

| Field | Value |
| --- | --- |
| Precondition | `acquire_continuation_and_run`'s `run` future is dropped before completion (the connection task is cancelled) rather than returning or panicking; the shard still believes it is executing the script's sub-commands |
| Postcondition | (RULED) either (a) release becomes shard-acknowledged via a release token matched against the granted `txid`, so a stale/early release cannot free a lock the shard has since regranted to someone else, or (b) — the documented minimum, built — the cancellation precondition is stated explicitly in FM-VLL-002's Invariant and pinned by a forcing test that aborts mid-`run` and asserts the shard still refuses a second continuation while the lock is held, then succeeds once the release from the cancelled holder's guard-drop propagates. Today, `ContinuationGuard::drop` fires every `guard.release_txs` entry unconditionally on cancellation exactly as on normal completion, releasing the lock while shard-side execution (a separate channel-driven task) may still be running — option (a)'s shard-acknowledged release is not built; option (b) is |
| Source | `frogdb-server/crates/vll/src/coordinator.rs:138` (`impl Drop for ContinuationGuard`), `frogdb-server/crates/server/src/acceptor.rs:357-361` (connections spawned with no stored `JoinHandle` — today's exposure is small only because nothing aborts a connection task) |
| Rulings | [issue 10](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/10-txn-vll-advisory-sweep.md) — option (b), the documented minimum, forced by `cancelling_the_holder_mid_run_releases_the_lock_with_no_shard_acknowledgement` (`coordinator.rs`) |

## TR-VLL-022 — Wound-wait resolves an SCA scatter collision

| Field | Value |
| --- | --- |
| Precondition | `try_grant` runs for a pending op and finds no SCA-order blocker (TR-VLL-002) on any key, but at least one key carries a conflicting **granted** intent belonging to a **higher** `txid` — the requester is older than a holder in its way. Rule-2 blocking can only ever be a younger holder: a lower-`txid` blocker is already caught by the SCA order check, so "blocked by rule 2" and "blocked by a junior" are the same condition |
| Postcondition | `try_grant` returns `GrantOutcome::WoundYounger(victims)` (sorted, deduplicated — a victim holding several of the requested keys is named once) and grants nothing. The requester stays `Pending`; each victim still in `shard.tx_queue` is sent `VllError::Wounded` on `op.wound_tx`. The wound is **advisory**: the shard releases no locks and removes no op of its own accord, because a victim may already have a phase-3 `VllExecute` in flight that cannot be taken back — the victim's coordinator unwinds through the ordinary abort path (TR-VLL-007), and that abort is what hands the lock over. A victim already dequeued for execution is past the point of no return and is not notified at all (its op, and with it `op.wound_tx`, has left the queue), so the older requester waits for its release. Coordinator-side the wound aborts every participant and surfaces as `ScatterError::Wounded { shard_id }` (`is_wound() = true`), which `ScatterGatherExecutor::execute` retries **under the same txid** (see [LV-VLL-002](#lv-vll-002--a-colliding-pair-of-scatters-always-makes-progress)); past `MAX_WOUND_RETRIES` the client gets `-BUSY wounded by an older transaction; retry`. The wound window is phase 2 only: `scatter` drops its `RevocationWatch` before dispatching execute, so a wound that arrives after the point of no return finds nobody listening and the victim finishes normally |
| Source | `frogdb-server/crates/vll/src/lock_table.rs` (`GrantOutcome`, `try_grant`), `frogdb-server/crates/vll/src/queue.rs` (`VllPendingOp::wound`), `frogdb-server/crates/vll/src/shard.rs` (`try_acquire_for`'s `WoundYounger` arm), `frogdb-server/crates/vll/src/coordinator.rs` (phase-2 `select!` on `RevocationWatch`, `ScatterError::Wounded`), `frogdb-server/crates/server/src/scatter/executor.rs` (`MAX_WOUND_RETRIES`, same-txid retry) |
| Rulings | [issue 14](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/14-sca-wound-wait-restores-acyclicity.md) — TR-VLL-014's ruling extended from the continuation lock to the SCA path; the advisory (rather than shard-side-release) shape is this row's deviation from the issue's literal text, forced by the phase-3 race |

## How to read a row

Fields as in [txn.md](/specifications/txn/#how-to-read-a-row). `Outcome variant`
here names the `ShardReadyResult` the shard sends and the coordinator error it becomes.

---

## FM-VLL-001 — SCA request refused while a continuation lock is held

| Field | Value |
|---|---|
| Trigger | A scatter (SCA) lock request reaches a shard whose continuation lock is held. The owner routes its own work through `ScriptSubCommand`, never SCA, so such a request is always from a different connection. |
| Observable | The shard replies `Failed(ShardBusy)` immediately; the coordinator aborts every participant and the client sees `-BUSY shard busy with continuation lock; retry`. |
| NOT observable | The request being queued behind the lock — it would interleave with the owner's sub-commands and break isolation; the request being granted; queue depth or the lock table changing. |
| Invariant | `enqueue_lock_request` short-circuits on `continuation_held_or_pending()` before `ensure_initialized`, so no intent is declared and `queue_depth()` is unchanged — the rejection leaves nothing to clean up. The *pending* half of that predicate is [FM-VLL-004](#fm-vll-004--sca-request-refused-while-a-continuation-request-is-parked). |
| Outcome variant | `ShardReadyResult::Failed(VllError::ShardBusy)` → `ScatterError::LockFailed` |
| Forced by | `sca_lock_request_rejected_while_continuation_held` |
| Bug refs | none |

## FM-VLL-002 — continuation lock refused while another connection holds one

| Field | Value |
|---|---|
| Trigger | A second continuation-lock request (cross-shard Lua / MULTI) reaches a shard that already holds one for a different transaction, or that already has one parked waiting for its queue to drain ([FM-VLL-003](#fm-vll-003--continuation-lock-requested-while-the-shard-queue-has-not-drained)). |
| Observable | `Failed(ShardBusy)` from that shard; the client sees `-BUSY shard busy with continuation lock; retry` — the same retryable reply the scatter path gives for this condition ([FM-VLL-001](#fm-vll-001--sca-request-refused-while-a-continuation-lock-is-held)). The caller's primary work never runs. |
| NOT observable | Two owners of the same shard; a second request queueing behind the first (two parked requests would race for the same drain); the losing caller's already-granted locks on *other* shards staying held (nothing else would ever release them); `run` being invoked after a failed acquisition. |
| Invariant | The shard takes or parks a request only from `continuation_lock == None && pending_continuation == None` — one continuation claim per shard at a time, held or pending. Coordinator-side, every failure path drops `release_txs`, which fires the release signal for each shard that did grant; `acquire_continuation_and_run` owns the guard for the whole run, so the release happens whether the work returns, panics, **or is cancelled** — `ContinuationGuard::drop` fires every `release_txs` entry unconditionally, with no round trip to the shard confirming its sub-command execution actually stopped. The precondition for that unconditional release being safe is narrow and, today, load-bearing rather than enforced: nothing currently aborts a connection task out from under `run` (`frogdb-server/crates/server/src/acceptor.rs:358-360` spawns connections with no stored `JoinHandle`), so cancellation is not reachable in practice. If that ever changes — a future runtime path that does abort a connection task — the lock would release while the shard still believes the sub-command is executing, and only a shard-acknowledged release-by-token (matched against the granted `txid`, so a stale release cannot free a lock the shard has since regranted) would close the gap; see [TR-VLL-021](/specifications/vll/#tr-vll-021--continuation-lock-cancellation-mid-run-ruled-not-yet-built). |
| Outcome variant | `ShardReadyResult::Failed(VllError::ShardBusy)` → `ContinuationError::LockFailed` |
| Forced by | `continuation_lock_blocks_second_acquire`, `second_continuation_request_refused_while_one_is_parked`, `acquire_continuation_releases_partially_acquired_on_failure`, `acquire_continuation_and_run_skips_run_and_releases_on_failure`, `continuation_lock_shard_busy_maps_to_busy_reply`, `cancelling_the_holder_mid_run_releases_the_lock_with_no_shard_acknowledgement` |
| Bug refs | none |

## FM-VLL-003 — continuation lock requested while the shard queue has not drained

| Field | Value |
|---|---|
| Trigger | A continuation-lock request reaches a shard that is not drained — its VLL queue still holds SCA ops, or an op it dequeued is still executing. The shard can only be taken exclusively once both are clear. |
| Observable | Drained shard (queue never used, or already drained): the lock is granted synchronously. Otherwise the request is **parked** — the shard replies nothing yet, keeps processing messages, and grants the lock from the drain point (the last queued op's `release_after_execution`, or its `abort`). If the shard is still not drained `CONTINUATION_DRAIN_TIMEOUT` (2 s) after parking, the host's continuation-event arm fires and the request gets `Failed(LockTimeout)` — the client sees `-ERR lock acquisition failed: VLL lock acquisition timeout`. |
| NOT observable | The requesting call blocking, sleeping, or polling — it is synchronous and returns to the shard event loop immediately, so the queue it waits on can still drain (see the liveness note); the lock granted over ops still in the queue or over a dequeued op still executing (such an op releases its own locks, so the "exclusive" owner would not be alone); a lock, a parked request, or a stored release receiver left behind after the timeout — `continuation_lock_owner()` and `continuation_lock_snapshot()` must both stay empty and the shard must accept SCA work again, or every later SCA request on that shard would be refused by FM-VLL-001/FM-VLL-004 forever. Nor **the barrier outliving its requester**: a park refuses every other connection's work (`enqueue_lock_request` → `ShardBusy`), so once the requester has given up — its coordinator timed out, its connection dropped — the park must fall immediately rather than at the end of its drain budget. It has no one to grant to, and the coordinator's retries re-arm it each round, so a barrier held for a dead requester is one the shard never leaves. Every blocked state is leavable as soon as the reason for it is gone; the grant path already cancels this way (TR-VLL-012), and `next_continuation_event` selects on the parked `ready_tx.closed()` so the parked path matches it. |
| Invariant | The load-bearing guard is `grant_continuation` never installing a lock — or its release receiver — for a requester whose `ready_tx` has already closed, whether that grant is attempted at the original call, at a later drain point, or racing a timeout. That is what makes it safe for the deadline to be served by the same `next_continuation_event` host-loop arm that also serves the release signal: whichever side loses the race installs nothing, correctly, because nobody is left listening. Around that guard: `request_continuation_lock` is not `async` — it grants immediately when `is_drained()` (queue empty *and* no dequeued op outstanding), else stores a `PendingContinuation` carrying the caller's `ready_tx`/`release_rx` and a `deadline`, and every grant, immediate or from a drain point, goes through `grant_continuation`. |
| Outcome variant | `ShardReadyResult::Failed(VllError::LockTimeout)` → `ContinuationError::LockFailed` |
| Forced by | `continuation_lock_parks_then_grants_when_the_queue_drains`, `continuation_lock_times_out_when_the_queue_never_drains`, `continuation_lock_acquires_once_queue_drains`, `continuation_lock_parks_while_a_dequeued_op_is_still_executing`, `parked_continuation_deadline_survives_cancellation`, `an_abandoned_park_drops_immediately_instead_of_holding_the_barrier`, `continuation_request_does_not_stall_the_shard_event_loop`, `continuation_drain_timeout_stays_below_the_coordinators_acquisition_timeout` |
| Bug refs | [issue 02](https://github.com/frogdb/frogdb/blob/main/.scratch/hardening/issues/done/02-continuation-drain-wait-blocks-event-loop.md), [issue 26](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/26-distsys-review-minors-sweep.md) (MIN-13) — a parked request whose requester had gone held the shard's drain barrier up until the 2 s deadline, re-armed on every coordinator retry, refusing every other connection's SCA work on behalf of nobody |

> **Liveness note.** The drain wait used to run *inside* the shard's own event loop
> (`acquire_continuation_lock` awaited from `handle_vll_continuation_lock`), and the queue only
> drains when that loop processes a `VllExecute` — so a queue that was non-empty on entry could
> never drain during the wait: the outcome was always `LockTimeout` after the full 2 s, during
> which the shard processed nothing. Parking inverts the dependency: the request returns to the
> event loop, the loop drains the queue, and the drain grants the lock. The 2 s deadline is now a
> real timeout (only reached when the queue genuinely does not drain). It is *derived from* the
> coordinator's `DEFAULT_LOCK_ACQUISITION_TIMEOUT` (4 s, currently halved) so the two constants
> cannot drift apart in a later edit — a tuning choice that usually lets the shard, rather than
> the coordinator, resolve and clean up after its own parked request first. That ordering is a
> liveness nicety, not the safety property: even if the coordinator gave up first and the
> caller's `ready_tx` closed before the shard's deadline fired, `grant_continuation` still
> declines to install a lock nobody is listening for (FM-VLL-003's Invariant, above) — so a lost
> race is harmless regardless of which side times out first.

## FM-VLL-004 — SCA request refused while a continuation request is parked

| Field | Value |
|---|---|
| Trigger | A scatter (SCA) lock request reaches a shard that has a continuation-lock request parked waiting for its queue to drain ([FM-VLL-003](#fm-vll-003--continuation-lock-requested-while-the-shard-queue-has-not-drained)). |
| Observable | Same as [FM-VLL-001](#fm-vll-001--sca-request-refused-while-a-continuation-lock-is-held): `Failed(ShardBusy)` immediately, `enqueue_failed`, and the client sees `-BUSY shard busy with continuation lock; retry`. Once the parked request is granted or times out, the shard accepts SCA work again. |
| NOT observable | The request being enqueued behind the parked continuation — new arrivals would keep the queue non-empty and starve the drain until the deadline, turning a transient wait into a guaranteed `LockTimeout`; queue depth or the lock table changing. |
| Invariant | Parking raises a drain barrier: the queue can only shrink while a request is parked, so the drain terminates (each queued op either executes or is aborted by its own coordinator) or the deadline fires. The refusal shares FM-VLL-001's short-circuit, so it declares no intent and leaves nothing to clean up. |
| Outcome variant | `ShardReadyResult::Failed(VllError::ShardBusy)` → `ScatterError::LockFailed` |
| Forced by | `sca_lock_request_refused_while_a_continuation_request_is_parked` |
| Bug refs | [issue 02](https://github.com/frogdb/frogdb/blob/main/.scratch/hardening/issues/done/02-continuation-drain-wait-blocks-event-loop.md) |

---

## FM-VLL-005 — a granted op panics while executing

| Field | Value |
|---|---|
| Trigger | An op that already holds its locks panics inside `execute_scatter_part` — an `assert!`, an index, an overflow, anywhere below the shard boundary including inside a dependency. Round-2 issue 63 (`FT.SEARCH … LIMIT 0 0` reaching tantivy's `assert_ne!(limit, 0)`) was one such panic, reachable before authentication. |
| Observable | Both paths: the op's coordinator receives an error-shaped `PartialResult` for this shard — the same per-op shape [FM-VLL-001](#fm-vll-001--sca-request-refused-while-a-continuation-lock-is-held) uses, so the merge recognizes it — carrying `ERR internal error` and no data; `frogdb_shard_panics_isolated_total{site="vll_execute"}` increments and one `error!` names the shard, the op and the panic message. **Read path** (op declared `LockMode::Read`): the panic is isolated there, and the shard answers the next message normally. **Write path** (op declared `LockMode::Write`): the node additionally fail-stops — one line to stderr naming the txid and the panic, then a clean process exit with code 70 (`WRITE_PATH_PANIC_EXIT_CODE`, `EX_SOFTWARE`), distinct from a panic-unwind exit (101), the supervisor's `process::abort()` (134) and a requested shutdown (0), so an orchestrator can tell fail-stop from crash by exit code alone. The node comes back through ordinary startup recovery, which replays the WALs — no shard-level restart mechanism exists or is implied. |
| NOT observable | **Partial writes surviving a panic**: a node that half-applied a mutation does not keep serving reads or writes from the structures that mutation left behind, and a cross-shard op whose siblings applied is not answered from this shard's post-panic state — the escalation is what bounds that window, exactly as [TR-VLL-018](#tr-vll-018--scatter-phase-3-execute-dispatch-failure-unwinds-remaining-holders-only) relies on. Also not observable: the worker task dying (the supervisor turns that into `process::abort()`, so one op would take the node down by the wrong route); the op's key intents surviving in the lock table, or `executing_ops` staying incremented — either leaks the locks to a command that no longer exists and blocks every later request on those keys plus any parked continuation lock forever; the panic message reaching the client; a truncated result folding into the merge as success; a *read*-path panic ending the process. |
| Invariant | `dequeue_for_execution` and `release_after_execution` stay paired across an unwind: the panic is caught inside `handle_vll_execute` (`frogdb-server/crates/core/src/shard/vll.rs`), so the release runs on the panic path exactly as on the success path — the locks are freed before the escalation decision, so a fail-stop never leaves a lock table that recovery has to reason about. The write/read split reads the `LockMode` the op declared at enqueue (`VllShardState::release_after_panic`, `frogdb-server/crates/vll/src/shard.rs`), not a re-derivation from the operation payload. Isolation is a structural backstop, never a licence to leave the panicking arithmetic unfixed — a non-zero counter is always a bug, and on the write path it is also an outage. |
| Outcome variant | n/a — the locks were already granted; the failure is in execution, not acquisition. |
| Forced by | `a_panicking_vll_op_releases_its_locks_and_the_shard_keeps_serving`, `a_write_path_panic_escalates_to_process_fail_stop`, `a_read_path_panic_is_isolated_without_fail_stop`, `a_dequeued_op_carries_the_mode_it_declared` |
| Bug refs | [campaign-2 issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/hardening-2/issues/done/07-no-panic-isolation-at-the-shard-boundary.md), [round-2 issue 63](https://github.com/frogdb/frogdb/blob/main/.scratch/testing-improvements-round2/issues/done/63-ft-search-limit-0-0-panics-the-shard-worker.md) |

---

## FM-VLL-006 — two continuation requests collide

| Field | Value |
|---|---|
| Trigger | Two cross-shard scripts request continuation locks over overlapping shard sets. On at least one shard the arriving request's `txid` is *lower* than the claim already there — held, or parked waiting for the drain ([FM-VLL-003](#fm-vll-003--continuation-lock-requested-while-the-shard-queue-has-not-drained)). |
| Observable | The older transaction acquires. A younger *holder* receives `VllError::Wounded` on its revocation channel: its coordinator abandons `run`, drops the guard, releases every shard it held, and returns `ContinuationError::Revoked { reason: Wounded }`; the connection retries the whole script under the **same** txid, and only once `MAX_WOUND_RETRIES` is exhausted does the client see `-BUSY shard busy with continuation lock; retry`. A younger *parked* claim is failed outright with `Failed(Wounded)` and becomes the same coordinator error. A younger *arriving* request is refused `Failed(ShardBusy)` and the incumbent is untouched ([FM-VLL-002](#fm-vll-002--continuation-lock-refused-while-another-connection-holds-one)). |
| NOT observable | Both parties aborting each other and retrying into the same interleaving — the mutual-abort livelock this row exists to exclude; a wounded caller retrying under a *fresh* txid (a retry that gave up its seniority could be wounded by every later arrival, forever); a wound clearing `shard.continuation_lock` shard-side before the holder releases (the shard cannot know the holder's sub-commands have stopped, and a lock freed under a live holder would let a second owner in); an older request losing to a younger claim merely because the younger one arrived first; more than one revocation notice per grant. |
| Invariant | `request_continuation_lock` resolves the *parked* slot before the *held* one, so an older arrival parking behind a wounded holder cannot overwrite an even older claim already waiting there. Every comparison is `txid >=` against the globally unique, totally ordered txid, so priority is antisymmetric and no wait cycle can form — this, not the callers' sorted shard order, is what buys deadlock-freedom under pipelined acquisition. `revoke_continuation` *takes* the sender, so the back-channel fires at most once per grant, and the wounding request parks so the `Released` arm's drain-point grant hands it the lock. |
| Outcome variant | `ShardReadyResult::Failed(VllError::Wounded)` (parked victim) or `VllError::Wounded` on `shard.continuation_revoke_tx` (holder) → `ContinuationError::Revoked { reason: Wounded }`, `is_wound() = true` |
| Forced by | `older_continuation_request_wounds_the_younger_holder`, `older_continuation_request_wounds_a_parked_younger_claim`, `younger_continuation_request_is_refused_without_wounding`, `wound_during_acquisition_gives_way_immediately`, `older_continuation_wins_over_a_younger_partial_holder` |
| Bug refs | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) — Finding H1, found by review rather than in production |

## FM-VLL-007 — a held continuation lock is revoked out from under its holder

| Field | Value |
|---|---|
| Trigger | `SCRIPT KILL`/`FUNCTION KILL` reaches a shard whose continuation lock is held, or the lock is still held `CONTINUATION_MAX_HOLD` (5 s) after it was granted. |
| Observable | The shard fires `VllError::Revoked` on the holder's revocation channel; the coordinator drops the `run` future, releases the lock on every participant, and returns `ContinuationError::Revoked` with `is_wound() = false`. The client that owned the script sees `-ERR Script stopped while holding shard <id>; its effects may be partial`. `SCRIPT KILL` succeeds on a shard whose own executor is idle but whose continuation lock was revoked — on every shard but the script's primary that is exactly the state a cross-shard script leaves behind — and still answers `-NOTBUSY No scripts in execution right now.` when there was neither a running script nor a lock to revoke. Sub-command writes already applied on sibling shards stand. |
| NOT observable | Rollback of writes already applied on sibling shards — a script is not a transaction, the same rule the local effect drain lives under; a reply that invites a retry (this was a deliberate stop, not contention, so it must not reuse `-BUSY`); the hold cap firing a second time for the same grant; the shard clearing `continuation_lock` at revocation time rather than when the holder's guard drops; `-NOTBUSY` after a lock really was revoked; other shards' locks staying held because only one shard was killed. |
| Invariant | One `oneshot::Sender<VllError>` per grant, installed with the lock and cleared with it, and *taken* by `revoke_continuation`, so it fires at most once. The hold deadline disarms itself when it fires (`continuation_hold_deadline = None`), so a revoked-but-not-yet-released lock cannot re-fire on every poll of the event arm. Revoking on one shard frees them all because the notice travels to the coordinator, which owns the guard for every participant — which is what lets `SCRIPT KILL`'s scatter stop at the first shard with something to say. |
| Outcome variant | `VllError::Revoked` on `shard.continuation_revoke_tx` → `ContinuationError::Revoked { reason: Revoked }`, `is_wound() = false` |
| Forced by | `revoking_a_held_continuation_notifies_its_holder`, `revoking_an_unheld_continuation_reports_no_holder`, `continuation_hold_cap_revokes_the_holder_once`, `continuation_revoked_mid_run_abandons_the_work_and_releases` |
| Bug refs | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) — Finding H2 |

## FM-VLL-008 — a scatter phase fails partway through

| Field | Value |
|---|---|
| Trigger | One participant fails while the others are already committed to the round: phase-1 dispatch of `VllLockRequest` fails after earlier participants declared intents (TR-VLL-016); phase-2's ready wait fails or times out while earlier participants hold granted locks (TR-VLL-017); phase-3 execute dispatch fails after earlier participants already received `VllExecute` (TR-VLL-018). |
| Observable | The whole scatter fails with the phase's own `ScatterError` and the client sees that error's reply. Every participant that declared an intent or holds a granted lock **and has not received execute** is aborted, addressed by its real shard id. Participants that already received execute are left alone: they release their own locks when they finish. |
| NOT observable | An abort addressed by loop *position* rather than real shard id — participant sets are sparse, so position `1` is routinely not shard `1`, and the wrong shard would be freed while the right one stayed locked; a participant left holding granted locks once the error has surfaced to the client; an abort sent to a participant that already received execute (it would double-free a lock the executing op still needs); the phase-3 partial-commit window persisting on a live node (the only way `send_execute` fails is a dropped shard receiver, which is node-fatal — TR-VLL-018). |
| Invariant | Each phase unwinds exactly the set it created, and the unwind is driven from the participants vec, so every abort carries a real shard id. Phase 3 is the boundary: before it, "not executed" is knowable and the round unwinds; at and after it the round is past the point of no return and reports rather than unwinds ([FM-VLL-009](#fm-vll-009--a-scatter-gather-wait-times-out-after-execute-was-dispatched)). |
| Outcome variant | `ScatterError::ShardUnavailable` (phases 1 and 3), `ScatterError::LockFailed`/`LockChannelClosed`/`LockTimeout` (phase 2) → `-ERR shard unavailable`, `-BUSY shard busy with continuation lock; retry`, `-ERR VLL lock acquisition failed` |
| Forced by | `phase1_dispatch_failure_aborts_shards_already_holding_intents`, `phase2_failure_aborts_real_shard_ids_for_sparse_participants`, `phase3_failure_aborts_remaining_holders_not_positions` |
| Bug refs | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) — Finding H3 |

## FM-VLL-009 — a scatter gather wait times out after execute was dispatched

| Field | Value |
|---|---|
| Trigger | Phase 4's wait for a participant's `PartialResult` exceeds the request's timeout. Phase 4 is entered only once phase-3 dispatch succeeded for *every* participant, so each one has already received `VllExecute`. |
| Observable | `ScatterError::ResultAmbiguous { shard_id, applied, unknown }` — `applied` names the participants that answered, `unknown` the ones whose fate the timeout did not settle. The executor logs both sets with the txid and replies `-ERR VLL execution timeout; outcome unknown`. |
| NOT observable | The timeout resolving the op to committed or aborted — that is wall-clock-as-correctness, the banned anti-pattern, and the whole point of the AMBIGUOUS shape; a reply that claims the op was aborted, or that reuses the `-ERR VLL lock acquisition failed` string the never-accepted failures share ([FM-VLL-008](#fm-vll-008--a-scatter-phase-fails-partway-through)); an abort sent to a participant that already received execute; an empty `unknown` set (a timeout with nothing unresolved is not this failure mode); `applied` folding silently into the merged reply as if the op had succeeded. |
| Invariant | Mirrors `FM-TXN-032`'s possibly-applied vs. definitely-not split. Past phase 3 there is nothing left to unwind *to*, so the coordinator reports which shards are unresolved instead of picking an outcome for them. "Never ran" is not reachable here without a shard worker that is gone, and that is node-fatal ([FM-VLL-005](#fm-vll-005--a-granted-op-panics-while-executing), TR-VLL-018) — so `unknown` genuinely means possibly-applied, never definitely-not. |
| Outcome variant | `ScatterError::ResultAmbiguous { shard_id, applied, unknown }` → `-ERR VLL execution timeout; outcome unknown` |
| Forced by | `phase4_gather_timeout_reports_which_shards_are_unknown` |
| Bug refs | [issue 07](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/07-vll-continuation-package.md) — Finding H3 |

## FM-VLL-010 — two scatters win the same shards in opposite orders

| Field | Value |
|---|---|
| Trigger | Two cross-shard scatters (e.g. two `MSET`s) touch an overlapping shard set. Phase-1 dispatch is pipelined — every `VllLockRequest` goes out before any ready signal is awaited — so each transaction can be granted on a shard the other still needs, and on at least one of those shards the requester's `txid` is *lower* than the granted holder's. |
| Observable | The older transaction acquires and commits. The younger holder receives `VllError::Wounded` on `op.wound_tx`; its coordinator aborts every participant it dispatched to and returns `ScatterError::Wounded { shard_id }`, and `ScatterGatherExecutor::execute` re-runs the whole scatter under the **same** txid. Only once `MAX_WOUND_RETRIES` is exhausted does the client see `-BUSY wounded by an older transaction; retry`. A conflict that respects the txid order still parks silently (TR-VLL-002), and the collision resolves without any acquisition timeout elapsing. |
| NOT observable | Both scatters parking on each other until phase 2's acquisition timeout fires on both and both abort — the deadlock this row exists to exclude; a wounded caller retrying under a *fresh* txid (a retry that gave up its seniority could be wounded by every later arrival, forever); an older requester being wounded by a younger arrival; the shard releasing a victim's locks or dropping its op on its own say-so (the victim may hold a phase-3 execute the shard cannot take back, and a lock freed under a live executor would let a second writer in); a wound reaching an op already dequeued for execution; more than one wound notice per op; a wound decided by wall clock or arrival order rather than by `txid`. |
| Invariant | Every comparison is against the globally unique, totally ordered `txid` (`lock_table.intents` is keyed by it, so "granted holders younger than me" is the `range(txid..)` suffix), so priority is antisymmetric and the wait-for graph has no cycles: an edge survives only when it points from younger to older. `VllPendingOp::wound` *takes* the sender, so the back-channel fires at most once per op, and the op leaving `shard.tx_queue` at `dequeue_for_execution` is what makes the point of no return unwoundable. On the coordinator side the wound window is exactly phase 2 — the `RevocationWatch` is dropped before the first `VllExecute` — so a wound can only ever cancel a transaction that has applied nothing. |
| Outcome variant | `VllError::Wounded` on `op.wound_tx` → `ScatterError::Wounded { shard_id }`, `is_wound() = true` → retry under the same txid, or `-BUSY wounded by an older transaction; retry` past `MAX_WOUND_RETRIES` |
| Forced by | `lower_txid_writer_wounds_a_granted_higher_reader`, `higher_txid_writer_waits_for_a_granted_lower_reader_without_wounding`, `an_sca_block_on_one_key_suppresses_wounds_on_the_others`, `a_victim_holding_several_keys_is_named_once`, `a_younger_intent_that_holds_nothing_is_not_wounded`, `wounding_an_op_notifies_its_coordinator_exactly_once`, `an_older_sca_request_wounds_the_younger_holder_in_its_way`, `a_younger_sca_request_waits_without_wounding_the_older_holder`, `an_executing_op_is_not_wounded`, `opposite_shard_orders_resolve_by_seniority_instead_of_deadlocking`, `only_a_wound_is_a_retryable_scatter_error`, `only_a_wound_is_a_retryable_continuation_error` |
| Bug refs | [issue 14](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/14-sca-wound-wait-restores-acyclicity.md) — found by review of the wound-wait ruling issue 07 landed for the continuation lock, which left the SCA path on the unordered rule it replaced |

## FM-VLL-011 — receiver waits are timed one at a time and sum

| Field | Value |
|---|---|
| Trigger | A request touches several shards and the coordinator awaits their answers in a sequential loop: phase 2's ready signals, phase 4's execute results, and `acquire_continuation`'s ready signals. Each shard is slow but answers — a contended lock table, a saturated shard, a large scatter — so no single wait ever expires on its own. |
| Observable | The request as a whole ends within `acquisition.timeout` of the moment it started, whichever receiver it happened to be waiting on: phase 2 gives `ScatterError::LockTimeout` (aborting every participant, TR-VLL-017), phase 4 gives `ScatterError::ResultAmbiguous` (TR-VLL-019), `acquire_continuation` gives `ContinuationError::LockTimeout`. The shard the deadline caught is the one named. |
| NOT observable | An observed bound that scales with participant count — the pre-fix `for … { timeout(request.timeout, rx) }` started a fresh *relative* timeout only once the loop reached each receiver, so N shards each answering just under the timeout ran for `N × timeout` (16 shards at the 4 s default: 64 s in phase 2 and another 64 s in phase 4, a request hanging over a minute against a 4 s setting). Also not observable: phase 4 restarting the budget after phase 2 spent it; a deadline recomputed, extended, or refreshed anywhere between `scatter` entry and its return; a participant that answers before the deadline being failed because an earlier one was slow. |
| Invariant | One request carries exactly one deadline, computed once at entry as `start + acquisition.timeout` and compared against by every wait beneath it (gRPC and CockroachDB's DistSender pass an absolute deadline down the call tree for the same reason). Sequential waits against a fixed absolute point cannot sum. The deadline is monotonic (`tokio::time::Instant`) and request-scoped: it bounds waiting, and never decides an op's outcome — that distinction is what keeps it clear of the no-wall-clock-in-state rule and is why a phase-4 expiry still reports AMBIGUOUS rather than aborting ([FM-VLL-009](#fm-vll-009--a-scatter-gather-wait-times-out-after-execute-was-dispatched)). Complementary to [FM-VLL-010](#fm-vll-010--two-scatters-win-the-same-shards-in-opposite-orders): wound-wait removes the deadlock *cycles* that made the timeout the only exit; this row bounds the honest-slowness case wound-wait cannot see. The bound is per `scatter` call: a wound retry is a new request and takes a new deadline, so a wounded caller's total wait is bounded by `MAX_WOUND_RETRIES` deadlines — one bound per attempt, still never one per participant. |
| Outcome variant | `ScatterError::LockTimeout { shard_id }` / `ScatterError::ResultAmbiguous { shard_id, applied, unknown }` / `ContinuationError::LockTimeout { shard_id }`, each at most `acquisition.timeout` after request entry |
| Forced by | `phase2_receiver_waits_share_one_absolute_deadline`, `phase4_gather_shares_the_request_deadline`, `continuation_acquisition_waits_share_one_absolute_deadline` |
| Bug refs | [issue 22](https://github.com/frogdb/frogdb/blob/main/.scratch/spec-gaps/issues/done/22-scatter-carries-one-absolute-deadline.md) — distsys-review MAJ-20 |

---

## LV-VLL-001 — a colliding pair of continuations always makes progress

| Field | Value |
|---|---|
| Property | For two continuation acquisitions colliding on an overlapping shard set, one of them runs to completion, and it is the older (lower-`txid`) one. The loser's retry keeps its txid, so with each round it only grows more senior relative to new arrivals — a stream of fresh cross-shard scripts cannot starve it. |
| Assumptions | Txids are globally unique and totally ordered (`next_txid`); a caller that sees `is_wound()` retries under the same txid rather than minting a new one; a wounded holder eventually drops its guard, which is what releases the lock the winner is parked on. Bounded by `MAX_WOUND_RETRIES` in the connection handler: past that the caller gets a retryable `-BUSY` and the *client* owns the next attempt, so this is a guarantee about the lock protocol, not an unbounded promise the server keeps on the caller's behalf. |
| Source | `frogdb-server/crates/vll/src/shard.rs` (`request_continuation_lock`), `frogdb-server/crates/server/src/connection/scripting/eval.rs` (`MAX_WOUND_RETRIES`) |
| Forced by | `older_continuation_wins_over_a_younger_partial_holder` |

## LV-VLL-002 — a colliding pair of scatters always makes progress

| Field | Value |
|---|---|
| Property | For two scatters colliding on an overlapping shard set, one of them commits, and it is the older (lower-`txid`) one. The loser's retry keeps its txid, so with each round it only grows more senior relative to new arrivals — a stream of fresh cross-shard writes cannot starve it. Neither side waits out the phase-2 acquisition timeout: the collision is settled by priority, not by the clock. |
| Assumptions | Txids are globally unique and totally ordered (`next_txid`); a caller that sees `is_wound()` retries under the same txid rather than minting a new one; a wounded coordinator's abort reaches every participant, which is what releases the lock the winner is parked on. Bounded by `MAX_WOUND_RETRIES` in `ScatterGatherExecutor::execute`: past that the client gets a retryable `-BUSY` and owns the next attempt, so this is a guarantee about the lock protocol, not an unbounded promise the server keeps on the caller's behalf. |
| Source | `frogdb-server/crates/vll/src/lock_table.rs` (`try_grant`), `frogdb-server/crates/vll/src/shard.rs` (`try_acquire_for`), `frogdb-server/crates/server/src/scatter/executor.rs` (`MAX_WOUND_RETRIES`) |
| Forced by | `opposite_shard_orders_resolve_by_seniority_instead_of_deadlocking` |
