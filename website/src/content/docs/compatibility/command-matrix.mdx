---
title: Command matrix
description: Per-command supported / partial / unsupported status for the Redis command set, generated from the running command registry.
---

import { LinkCard, CardGrid } from '@astrojs/starlight/components';
import matrix from '../../../data/command-matrix.json';
import CommandMatrixSummary from '../../../components/CommandMatrixSummary.astro';
import CommandMatrix from '../../../components/CommandMatrix.astro';

This matrix is generated from FrogDB's running command registry joined with the Redis {matrix.target_redis_version} command set and the regression-test exclusions, so it cannot drift from the binary. **Supported** means the command is implemented and its regression coverage is intact. **Partial** means the command runs but has a documented behavioral delta or a meaningful set of excluded regression tests. **Unsupported** means the command exists in Redis {matrix.target_redis_version} but is either absent from FrogDB or present only as a deliberate no-op stub. Standard command behavior is documented at [redis.io](https://redis.io/docs/); FrogDB documents only its deltas on [Overview & differences](/compatibility/overview/).

## Summary

<CommandMatrixSummary />

FrogDB extensions are FrogDB-original commands with no Redis equivalent (for example the `ES.*` event-sourcing family); they are counted separately above and documented under Extensions.

## Matrix

Filter by command or family name, or narrow to a single status. The full table renders without JavaScript.

<CommandMatrix />

## How this is generated

A registry-dump generator constructs the command registry exactly as the server does and emits every command with its arity and flags to `website/src/data/commands.json`. A join step then computes one row per command over the union of the registry, the vendored Redis {matrix.target_redis_version} command list, and the regression-test exclusions in `website/src/data/compat-exclusions.json`, writing `website/src/data/command-matrix.json`. Both files are regenerated by `just docs-build` and verified in CI with a `--check` mode, so a registry change that is not regenerated fails the build. The excluded-test evidence behind "partial" statuses is detailed on [Test suite results](/compatibility/test-suite/); the broader strategy is on [Testing methodology](/compatibility/testing-methodology/).

## See also

<CardGrid>
  <LinkCard title="Overview & differences" href="/compatibility/overview/" description="FrogDB's compatibility target and behavioral differences" />
  <LinkCard title="Test suite results" href="/compatibility/test-suite/" description="Redis regression coverage and intentional exclusions" />
</CardGrid>
