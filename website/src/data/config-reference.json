{
  "_generated": {
    "warning": "DO NOT EDIT — this file is auto-generated from Rust source code",
    "source": "frogdb-server/crates/config/src/",
    "regenerate": "just docs-gen"
  },
  "sections": {
    "acl": {
      "description": "ACL configuration.",
      "fields": [
        {
          "name": "aclfile",
          "type": "string",
          "default": "",
          "description": "Path to the ACL file for SAVE/LOAD operations.\nIf empty, ACL SAVE/LOAD will return an error.",
          "config_param": "aclfile",
          "mutable": false
        },
        {
          "name": "log-max-len",
          "type": "integer",
          "default": 128,
          "description": "Maximum number of entries in the ACL LOG.",
          "config_param": "acllog-max-len",
          "mutable": true
        }
      ]
    },
    "admin": {
      "description": "Admin API configuration.",
      "fields": [
        {
          "name": "bind",
          "type": "string",
          "default": "127.0.0.1",
          "description": "Bind address for the admin RESP protocol listener.",
          "config_param": "admin-bind",
          "mutable": false
        },
        {
          "name": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Whether the admin API is enabled.",
          "config_param": "admin-enabled",
          "mutable": false
        },
        {
          "name": "port",
          "type": "integer",
          "default": 6382,
          "description": "Port for the admin RESP protocol listener.",
          "config_param": "admin-port",
          "mutable": false
        }
      ]
    },
    "blocking": {
      "description": "Blocking commands configuration.",
      "fields": [
        {
          "name": "max-blocked-connections",
          "type": "integer",
          "default": 50000,
          "description": "Maximum total blocked connections (0 = unlimited)."
        },
        {
          "name": "max-waiters-per-key",
          "type": "integer",
          "default": 10000,
          "description": "Maximum waiters per key (0 = unlimited)."
        }
      ]
    },
    "cluster": {
      "description": "Cluster configuration.",
      "fields": [
        {
          "name": "auto-failover",
          "type": "boolean",
          "default": false,
          "description": "Enable automatic failover when a primary fails.\nWhen enabled, the leader will automatically promote a replica to primary\nif the primary becomes unreachable.",
          "config_param": "cluster-auto-failover",
          "mutable": false
        },
        {
          "name": "client-addr",
          "type": "string",
          "default": "",
          "description": "Address for client connections (host:port).\nDefaults to server.bind:server.port if not specified."
        },
        {
          "name": "cluster-bus-addr",
          "type": "string",
          "default": "127.0.0.1:16379",
          "description": "Address for cluster bus (Raft) communication.\nTypically server port + 10000 (e.g., 16379 for 6379)."
        },
        {
          "name": "connect-timeout-ms",
          "type": "integer",
          "default": 5000,
          "description": "Connection timeout for cluster bus in milliseconds."
        },
        {
          "name": "data-dir",
          "type": "string",
          "default": "./frogdb-cluster",
          "description": "Directory for storing cluster state (Raft logs, snapshots).",
          "config_param": "cluster-data-dir",
          "mutable": false
        },
        {
          "name": "election-timeout-ms",
          "type": "integer",
          "default": 1000,
          "description": "Election timeout in milliseconds.\nA leader must receive heartbeats within this time or election starts."
        },
        {
          "name": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Whether cluster mode is enabled.",
          "config_param": "cluster-enabled",
          "mutable": false
        },
        {
          "name": "fail-threshold",
          "type": "integer",
          "default": 5,
          "description": "Number of consecutive failures before marking a node as FAIL."
        },
        {
          "name": "heartbeat-interval-ms",
          "type": "integer",
          "default": 250,
          "description": "Heartbeat interval in milliseconds.\nLeader sends heartbeats at this interval."
        },
        {
          "name": "initial-nodes",
          "type": "array",
          "default": [],
          "description": "Initial cluster nodes to connect to (for joining existing cluster).\nFormat: [\"host1:port1\", \"host2:port2\"]"
        },
        {
          "name": "node-id",
          "type": "integer",
          "default": 0,
          "description": "This node's unique ID (0 = auto-generate from timestamp)."
        },
        {
          "name": "replica-priority",
          "type": "integer",
          "default": 100,
          "description": "Priority for replica promotion during auto-failover.\nLower values are preferred. 0 means this replica will never be promoted.",
          "config_param": "replica-priority",
          "mutable": false
        },
        {
          "name": "request-timeout-ms",
          "type": "integer",
          "default": 10000,
          "description": "Request timeout for cluster bus RPCs in milliseconds."
        },
        {
          "name": "self-fence-on-quorum-loss",
          "type": "boolean",
          "default": true,
          "description": "Reject write commands when this node cannot form a quorum with reachable nodes.\nWhen enabled, writes return CLUSTERDOWN if quorum is lost, preventing\nsplit-brain data divergence. Reads remain available.",
          "config_param": "cluster-self-fence-on-quorum-loss",
          "mutable": false
        }
      ]
    },
    "compat": {
      "description": "Compatibility configuration for Redis protocol behavior.",
      "fields": [
        {
          "name": "strict-config",
          "type": "boolean",
          "default": false,
          "description": "When true, CONFIG GET/SET returns errors for Redis params that FrogDB\ntreats as no-ops. When false, silently accepts them for\ncompatibility with Redis clients/tools that set these params."
        }
      ]
    },
    "debug-bundle": {
      "description": "Debug bundle configuration.\n\nControls the generation and storage of diagnostic bundles for troubleshooting.",
      "fields": [
        {
          "name": "bundle-ttl-secs",
          "type": "integer",
          "default": 3600,
          "description": "Bundle TTL in seconds before automatic cleanup."
        },
        {
          "name": "directory",
          "type": "string",
          "default": "frogdb-data/bundles",
          "description": "Directory for storing bundles."
        },
        {
          "name": "max-bundles",
          "type": "integer",
          "default": 10,
          "description": "Maximum number of bundles to retain."
        },
        {
          "name": "max-slowlog-entries",
          "type": "integer",
          "default": 256,
          "description": "Maximum slowlog entries to include in bundles."
        },
        {
          "name": "max-trace-entries",
          "type": "integer",
          "default": 100,
          "description": "Maximum trace entries to include in bundles."
        }
      ]
    },
    "http": {
      "description": "HTTP server configuration for the unified observability/admin endpoint.",
      "fields": [
        {
          "name": "bind",
          "type": "string",
          "default": "127.0.0.1",
          "description": "Bind address for the HTTP server.",
          "config_param": "http-bind",
          "mutable": false
        },
        {
          "name": "enabled",
          "type": "boolean",
          "default": true,
          "description": "Whether the HTTP server is enabled.",
          "config_param": "http-enabled",
          "mutable": false
        },
        {
          "name": "port",
          "type": "integer",
          "default": 9090,
          "description": "Port for the HTTP server.",
          "config_param": "http-port",
          "mutable": false
        },
        {
          "name": "token",
          "type": "string",
          "default": null,
          "description": "Optional bearer token for protected endpoints (/admin/*, /debug/*).\nWhen set, requests to these paths must include `Authorization: Bearer <token>`."
        }
      ]
    },
    "json": {
      "description": "JSON configuration.",
      "fields": [
        {
          "name": "max-depth",
          "type": "integer",
          "default": 128,
          "description": "Maximum nesting depth for JSON documents.",
          "config_param": "json-max-depth",
          "mutable": false
        },
        {
          "name": "max-size",
          "type": "integer",
          "default": 67108864,
          "description": "Maximum size in bytes for JSON documents.",
          "config_param": "json-max-size",
          "mutable": false
        }
      ]
    },
    "latency": {
      "description": "Latency testing configuration.",
      "fields": [
        {
          "name": "startup-test",
          "type": "boolean",
          "default": false,
          "description": "Run intrinsic latency test at startup before accepting connections."
        },
        {
          "name": "startup-test-duration-secs",
          "type": "integer",
          "default": 5,
          "description": "Duration of the startup latency test in seconds."
        },
        {
          "name": "warning-threshold-us",
          "type": "integer",
          "default": 2000,
          "description": "Warning threshold for intrinsic latency in microseconds.\nIf max latency exceeds this, a warning is logged."
        }
      ]
    },
    "latency-bands": {
      "description": "Latency bands configuration for SLO-focused monitoring.\n\nTracks cumulative request counts in configurable latency buckets,\nenabling direct SLO monitoring without external aggregation.",
      "fields": [
        {
          "name": "bands",
          "type": "array",
          "default": [
            1,
            5,
            10,
            50,
            100,
            500
          ],
          "description": "Latency band thresholds in milliseconds.\nRequests are counted in cumulative buckets (<=1ms, <=5ms, etc.)",
          "config_param": "latency-bands",
          "mutable": false
        },
        {
          "name": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Whether latency band tracking is enabled.",
          "config_param": "latency-bands-enabled",
          "mutable": false
        }
      ]
    },
    "logging": {
      "description": "Logging configuration.",
      "fields": [
        {
          "name": "file-path",
          "type": "string",
          "default": null,
          "description": "File path for log output. When set, logs are written to this file\nin addition to console output. Use `output = \"none\"` to disable console.",
          "config_param": "logfile",
          "mutable": false
        },
        {
          "name": "format",
          "type": "string",
          "default": "pretty",
          "description": "Log format (pretty, json)."
        },
        {
          "name": "level",
          "type": "string",
          "default": "info",
          "description": "Log level (trace, debug, info, warn, error).",
          "config_param": "loglevel",
          "mutable": true
        },
        {
          "name": "output",
          "type": "string",
          "default": "stdout",
          "description": "Console output destination.",
          "enum_values": [
            "stdout",
            "stderr",
            "none"
          ]
        },
        {
          "name": "per-request-spans",
          "type": "boolean",
          "default": false,
          "description": "Enable per-request tracing spans (cmd_read, cmd_execute, cmd_route, etc.).\nDisabled by default for production performance (~7% CPU savings).\nEnable for debugging or when distributed tracing is needed.",
          "config_param": "per-request-spans",
          "mutable": true
        },
        {
          "name": "rotation",
          "type": "string",
          "default": null,
          "description": "Log file rotation settings. Only meaningful when `file_path` is set."
        }
      ]
    },
    "memory": {
      "description": "Memory management configuration.",
      "fields": [
        {
          "name": "doctor-big-key-threshold",
          "type": "integer",
          "default": 1048576,
          "description": "Threshold in bytes for MEMORY DOCTOR big key detection.\nKeys larger than this will be flagged."
        },
        {
          "name": "doctor-imbalance-threshold",
          "type": "number",
          "default": 25.0,
          "description": "Threshold for shard memory imbalance detection (coefficient of variation).\nShards with memory CV higher than this will trigger a warning."
        },
        {
          "name": "doctor-max-big-keys",
          "type": "integer",
          "default": 100,
          "description": "Maximum number of big keys to report per shard in MEMORY DOCTOR."
        },
        {
          "name": "lfu-decay-time",
          "type": "integer",
          "default": 1,
          "description": "LFU decay time in minutes - counter decays by 1 every N minutes.",
          "config_param": "lfu-decay-time",
          "mutable": true
        },
        {
          "name": "lfu-log-factor",
          "type": "integer",
          "default": 10,
          "description": "LFU log factor - higher values make counter increment less likely.",
          "config_param": "lfu-log-factor",
          "mutable": true
        },
        {
          "name": "maxmemory",
          "type": "integer",
          "default": 0,
          "description": "Maximum memory limit in bytes. 0 means unlimited.\nCan use human-readable formats like \"100mb\", \"1gb\" in config files.",
          "config_param": "maxmemory",
          "mutable": true
        },
        {
          "name": "maxmemory-clients",
          "type": "string",
          "default": "0",
          "description": "Maximum memory allowed for all client buffers combined.\nSupports: \"0\" (disabled), absolute values like \"100mb\"/\"1gb\",\nor a percentage of maxmemory like \"5%\".",
          "config_param": "maxmemory-clients",
          "mutable": true
        },
        {
          "name": "maxmemory-policy",
          "type": "string",
          "default": "noeviction",
          "description": "Eviction policy when maxmemory is reached.\nOptions: noeviction, volatile-lru, allkeys-lru, volatile-lfu, allkeys-lfu,\n         volatile-random, allkeys-random, volatile-ttl",
          "config_param": "maxmemory-policy",
          "mutable": true
        },
        {
          "name": "maxmemory-samples",
          "type": "integer",
          "default": 5,
          "description": "Number of keys to sample when looking for eviction candidates.",
          "config_param": "maxmemory-samples",
          "mutable": true
        }
      ]
    },
    "metrics": {
      "description": "Metrics configuration.",
      "fields": [
        {
          "name": "enabled",
          "type": "boolean",
          "default": true,
          "description": "Whether metrics are enabled.",
          "config_param": "metrics-enabled",
          "mutable": false
        },
        {
          "name": "otlp-enabled",
          "type": "boolean",
          "default": false,
          "description": "Whether OTLP export is enabled.",
          "config_param": "metrics-otlp-enabled",
          "mutable": false
        },
        {
          "name": "otlp-endpoint",
          "type": "string",
          "default": "http://localhost:4317",
          "description": "OTLP endpoint URL.",
          "config_param": "metrics-otlp-endpoint",
          "mutable": false
        },
        {
          "name": "otlp-interval-secs",
          "type": "integer",
          "default": 15,
          "description": "OTLP push interval in seconds.",
          "config_param": "metrics-otlp-interval-secs",
          "mutable": false
        },
        {
          "name": "port",
          "type": "integer",
          "default": 9090,
          "description": "Port for the metrics HTTP server.",
          "config_param": "metrics-port",
          "mutable": false
        }
      ]
    },
    "monitor": {
      "description": "MONITOR command configuration.",
      "fields": [
        {
          "name": "channel-capacity",
          "type": "integer",
          "default": 4096,
          "description": "Bounded broadcast channel capacity (ring buffer size).\nSlow subscribers skip ahead rather than blocking the server."
        }
      ]
    },
    "persistence": {
      "description": "Persistence configuration.",
      "fields": [
        {
          "name": "batch-size-threshold-kb",
          "type": "integer",
          "default": 4096,
          "description": "Batch size threshold in KB before flushing.",
          "config_param": "batch-size-threshold-kb",
          "mutable": false
        },
        {
          "name": "batch-timeout-ms",
          "type": "integer",
          "default": 10,
          "description": "Batch timeout in milliseconds before flushing.",
          "config_param": "batch-timeout-ms",
          "mutable": true
        },
        {
          "name": "block-cache-size-mb",
          "type": "integer",
          "default": 256,
          "description": "RocksDB block cache size in MB.",
          "config_param": "block-cache-size-mb",
          "mutable": false
        },
        {
          "name": "bloom-filter-bits",
          "type": "integer",
          "default": 10,
          "description": "RocksDB bloom filter bits per key. Set to 0 to disable.",
          "config_param": "bloom-filter-bits",
          "mutable": false
        },
        {
          "name": "compaction-rate-limit-mb",
          "type": "integer",
          "default": 0,
          "description": "RocksDB compaction rate limit in MB/s. 0 means unlimited.",
          "config_param": "compaction-rate-limit-mb",
          "mutable": false
        },
        {
          "name": "compression",
          "type": "string",
          "default": "lz4",
          "description": "Compression type: \"none\", \"snappy\", \"lz4\", \"zstd\".",
          "config_param": "compression",
          "mutable": false
        },
        {
          "name": "data-dir",
          "type": "string",
          "default": "./frogdb-data",
          "description": "Directory for data files.",
          "config_param": "dir",
          "mutable": false
        },
        {
          "name": "durability-mode",
          "type": "string",
          "default": "periodic",
          "description": "Durability mode: \"async\", \"periodic\", or \"sync\".",
          "config_param": "durability-mode",
          "mutable": true
        },
        {
          "name": "enabled",
          "type": "boolean",
          "default": true,
          "description": "Whether persistence is enabled.",
          "config_param": "persistence-enabled",
          "mutable": false
        },
        {
          "name": "flush-compact-range",
          "type": "boolean",
          "default": true,
          "description": "Reclaim disk eagerly after FLUSHDB/FLUSHALL: follow the range tombstone\nwith an asynchronous DeleteFilesInRange + CompactRange over the cleared\ncolumn families. Without it, flushed SST bytes are only reclaimed when\na compaction happens to cover the range.",
          "config_param": "flush-compact-range",
          "mutable": false
        },
        {
          "name": "max-write-buffer-number",
          "type": "integer",
          "default": 4,
          "description": "Maximum number of RocksDB write buffers.",
          "config_param": "max-write-buffer-number",
          "mutable": false
        },
        {
          "name": "mode",
          "type": "string",
          "default": "rocksdb",
          "description": "WAL implementation to use: `\"rocksdb\"` (default, production) or `\"fake\"`\n(deterministic in-process sink for simulation tests). `\"fake\"` requires\n`enabled = true`."
        },
        {
          "name": "sync-interval-ms",
          "type": "integer",
          "default": 1000,
          "description": "Sync interval in milliseconds (for periodic mode).",
          "config_param": "sync-interval-ms",
          "mutable": true
        },
        {
          "name": "wal-failure-policy",
          "type": "string",
          "default": "continue",
          "description": "WAL failure policy: \"continue\" or \"rollback\".",
          "config_param": "wal-failure-policy",
          "mutable": true
        },
        {
          "name": "write-buffer-size-mb",
          "type": "integer",
          "default": 64,
          "description": "RocksDB write buffer size in MB.",
          "config_param": "write-buffer-size-mb",
          "mutable": false
        }
      ]
    },
    "replication": {
      "description": "Replication configuration.",
      "fields": [
        {
          "name": "ack-interval-ms",
          "type": "integer",
          "default": 1000,
          "description": "ACK interval - how often replicas send ACKs to primary (milliseconds).",
          "config_param": "repl-ack-interval-ms",
          "mutable": false
        },
        {
          "name": "connect-timeout-ms",
          "type": "integer",
          "default": 5000,
          "description": "Connection timeout for replica connecting to primary (milliseconds)."
        },
        {
          "name": "fullresync-cooldown-secs",
          "type": "integer",
          "default": 60,
          "description": "Cooldown seconds after proactive lag disconnect before allowing another."
        },
        {
          "name": "handshake-timeout-ms",
          "type": "integer",
          "default": 10000,
          "description": "Handshake timeout during replication setup (milliseconds)."
        },
        {
          "name": "min-replicas-timeout-ms",
          "type": "integer",
          "default": 5000,
          "description": "Timeout for min_replicas_to_write in milliseconds.\nIf replicas don't acknowledge within this time, the write still succeeds\nbut returns with fewer acknowledged replicas.",
          "config_param": "min-replicas-max-lag",
          "mutable": true
        },
        {
          "name": "min-replicas-to-write",
          "type": "integer",
          "default": 0,
          "description": "Minimum replicas required to acknowledge writes (for primary role).\nIf set > 0, writes will wait for this many replicas to acknowledge\nbefore returning success.",
          "config_param": "min-replicas-to-write",
          "mutable": true
        },
        {
          "name": "primary-host",
          "type": "string",
          "default": "",
          "description": "Primary host (for replica role).\nWhen role is \"replica\", this specifies the primary to connect to."
        },
        {
          "name": "primary-port",
          "type": "integer",
          "default": 6379,
          "description": "Primary port (for replica role)."
        },
        {
          "name": "reconnect-backoff-initial-ms",
          "type": "integer",
          "default": 100,
          "description": "Reconnection backoff - initial delay (milliseconds)."
        },
        {
          "name": "reconnect-backoff-max-ms",
          "type": "integer",
          "default": 30000,
          "description": "Reconnection backoff - maximum delay (milliseconds)."
        },
        {
          "name": "replica-freshness-timeout-ms",
          "type": "integer",
          "default": 3000,
          "description": "Freshness timeout for replica ACKs (ms).\nIf no replica ACKs within this window, the primary fences itself.\nShould be >= 3x ack_interval_ms to tolerate missed ACKs.",
          "config_param": "replica-freshness-timeout-ms",
          "mutable": false
        },
        {
          "name": "replica-write-timeout-ms",
          "type": "integer",
          "default": 5000,
          "description": "Write timeout for streaming to replicas (ms). 0 = disabled.\nForces TCP disconnect when iptables drops packets."
        },
        {
          "name": "replication-lag-threshold-bytes",
          "type": "integer",
          "default": 0,
          "description": "Max replication lag in bytes before proactive disconnect. 0 = disabled.",
          "config_param": "replication-lag-threshold-bytes",
          "mutable": false
        },
        {
          "name": "replication-lag-threshold-secs",
          "type": "integer",
          "default": 0,
          "description": "Max replication lag in seconds (since last ACK) before proactive disconnect. 0 = disabled.",
          "config_param": "replication-lag-threshold-secs",
          "mutable": false
        },
        {
          "name": "role",
          "type": "string",
          "default": "standalone",
          "description": "Replication role: \"standalone\", \"primary\", or \"replica\".\n- standalone: No replication\n- primary: Accept replica connections\n- replica: Connect to a primary"
        },
        {
          "name": "self-fence-on-replica-loss",
          "type": "boolean",
          "default": true,
          "description": "Reject writes when primary loses all replica ACK freshness.\nPrevents zombie writes during network partitions.",
          "config_param": "self-fence-on-replica-loss",
          "mutable": false
        },
        {
          "name": "split-brain-buffer-max-mb",
          "type": "integer",
          "default": 64,
          "description": "Maximum memory in MB for the split-brain command buffer."
        },
        {
          "name": "split-brain-buffer-size",
          "type": "integer",
          "default": 10000,
          "description": "Maximum number of recent commands to buffer for split-brain detection."
        },
        {
          "name": "split-brain-log-enabled",
          "type": "boolean",
          "default": true,
          "description": "Enable split-brain discarded-writes logging (log-only).\n\nWhen a demoted primary diverged from the new primary, its divergent\nwrites are logged before resync. This flag controls ONLY that logging;\nit does not affect cluster behavior. Automatic Role Demotion during\nfailover always runs in cluster mode regardless of this setting — the\nkill-switch for cluster behavior is `cluster.enabled`, not this flag."
        },
        {
          "name": "state-file",
          "type": "string",
          "default": "replication_state.json",
          "description": "Replication state file path.\nStores replication ID and offset for partial sync recovery."
        }
      ]
    },
    "security": {
      "description": "Security configuration.",
      "fields": [
        {
          "name": "requirepass",
          "type": "string",
          "default": "",
          "description": "Legacy password for the default user (like Redis requirepass).\nIf set, clients must AUTH with this password before running commands.",
          "config_param": "requirepass",
          "mutable": true
        }
      ]
    },
    "server": {
      "description": "Server-specific configuration.",
      "fields": [
        {
          "name": "allow-cross-slot-standalone",
          "type": "boolean",
          "default": false,
          "description": "Allow cross-slot operations in standalone mode.\nWhen enabled, multi-key commands like MGET/MSET can operate across different\nhash slots using scatter-gather. MSETNX always requires same-slot."
        },
        {
          "name": "bind",
          "type": "string",
          "default": "127.0.0.1",
          "description": "Bind address.",
          "config_param": "bind",
          "mutable": false
        },
        {
          "name": "enable-debug-command",
          "type": "boolean",
          "default": false,
          "description": "Enable the DEBUG family of subcommands that are unsafe in production.\n\nCurrently gates `DEBUG SLEEP`, which parks the connection task for an\narbitrary duration and is a trivial denial-of-service vector if\nexposed to untrusted clients. Default: `false`. The test harness\ndefaults it to `true` so existing test-only DEBUG commands keep\nworking.",
          "config_param": "enable-debug-command",
          "mutable": false
        },
        {
          "name": "max-clients",
          "type": "integer",
          "default": 10000,
          "description": "Maximum number of simultaneous client connections (0 = unlimited).\nAdmin port connections are exempt from this limit.",
          "config_param": "maxclients",
          "mutable": true
        },
        {
          "name": "num-shards",
          "type": "integer",
          "default": 1,
          "description": "Number of shards (0 = auto-detect CPU cores).",
          "config_param": "num-shards",
          "mutable": false
        },
        {
          "name": "port",
          "type": "integer",
          "default": 6379,
          "description": "Listen port.",
          "config_param": "port",
          "mutable": false
        },
        {
          "name": "scatter-gather-timeout-ms",
          "type": "integer",
          "default": 5000,
          "description": "Timeout for scatter-gather operations in milliseconds.",
          "config_param": "scatter-gather-timeout-ms",
          "mutable": true
        },
        {
          "name": "sorted-set-index",
          "type": "string",
          "default": "skiplist",
          "description": "Sorted set index backend selection.",
          "enum_values": [
            "btreemap",
            "skiplist"
          ],
          "config_param": "sorted-set-index",
          "mutable": false
        }
      ]
    },
    "slowlog": {
      "description": "Slow query log configuration.",
      "fields": [
        {
          "name": "log-slower-than",
          "type": "integer",
          "default": 10000,
          "description": "Threshold in microseconds. Commands slower than this are logged.\nSet to 0 to log all commands, -1 to disable logging.",
          "config_param": "slowlog-log-slower-than",
          "mutable": true
        },
        {
          "name": "max-arg-len",
          "type": "integer",
          "default": 128,
          "description": "Maximum characters per argument before truncation.",
          "config_param": "slowlog-max-arg-len",
          "mutable": true
        },
        {
          "name": "max-len",
          "type": "integer",
          "default": 128,
          "description": "Maximum number of entries per shard.",
          "config_param": "slowlog-max-len",
          "mutable": true
        }
      ]
    },
    "snapshot": {
      "description": "Snapshot configuration.",
      "fields": [
        {
          "name": "max-snapshots",
          "type": "integer",
          "default": 5,
          "description": "Maximum number of snapshots to retain (0 = unlimited)."
        },
        {
          "name": "snapshot-dir",
          "type": "string",
          "default": "./snapshots",
          "description": "Directory for storing snapshots.",
          "config_param": "snapshot-dir",
          "mutable": false
        },
        {
          "name": "snapshot-interval-secs",
          "type": "integer",
          "default": 3600,
          "description": "Interval between automatic snapshots in seconds (0 = disabled).",
          "config_param": "snapshot-interval-secs",
          "mutable": false
        }
      ]
    },
    "status": {
      "description": "Status endpoint configuration for health thresholds.",
      "fields": [
        {
          "name": "connection-warning-percent",
          "type": "integer",
          "default": 90,
          "description": "Threshold percentage for connection warning (0-100).",
          "config_param": "status-connection-warning-percent",
          "mutable": false
        },
        {
          "name": "durability-lag-critical-ms",
          "type": "integer",
          "default": 30000,
          "description": "Durability lag critical threshold in milliseconds.",
          "config_param": "status-durability-lag-critical-ms",
          "mutable": false
        },
        {
          "name": "durability-lag-warning-ms",
          "type": "integer",
          "default": 5000,
          "description": "Durability lag warning threshold in milliseconds.",
          "config_param": "status-durability-lag-warning-ms",
          "mutable": false
        },
        {
          "name": "memory-warning-percent",
          "type": "integer",
          "default": 90,
          "description": "Threshold percentage for memory warning (0-100).",
          "config_param": "status-memory-warning-percent",
          "mutable": false
        }
      ]
    },
    "tiered-storage": {
      "description": "Tiered storage configuration for two-tier (hot/warm) storage.\n\nWhen enabled, eviction policies `tiered-lru` and `tiered-lfu` spill values\nto a RocksDB warm tier instead of deleting them.",
      "fields": [
        {
          "name": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Enable tiered storage. Requires persistence to be enabled."
        }
      ]
    },
    "tls": {
      "description": "TLS configuration section.",
      "fields": [
        {
          "name": "ca-file",
          "type": "string",
          "default": null,
          "description": "Path to the CA certificate file for client certificate verification (PEM format).\nRequired when `require_client_cert` is not `none`.",
          "config_param": "tls-ca-cert-file",
          "mutable": false
        },
        {
          "name": "cert-file",
          "type": "string",
          "default": "",
          "description": "Path to the server certificate file (PEM format).",
          "config_param": "tls-cert-file",
          "mutable": false
        },
        {
          "name": "ciphersuites",
          "type": "array",
          "default": [],
          "description": "Allowed ciphersuites, by rustls IANA name (case-insensitive), e.g.\n`TLS13_AES_256_GCM_SHA384` or `TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256`.\nEmpty means use rustls defaults. Applied by the server crate when\nbuilding the rustls Server/Client config; an unknown name or a set that\nexcludes every enabled protocol version fails loudly at startup.",
          "config_param": "tls-ciphersuites",
          "mutable": false
        },
        {
          "name": "client-cert-file",
          "type": "string",
          "default": null,
          "description": "Path to client certificate for outgoing replication/cluster connections.",
          "config_param": "tls-client-cert-file",
          "mutable": false
        },
        {
          "name": "client-key-file",
          "type": "string",
          "default": null,
          "description": "Path to client private key for outgoing replication/cluster connections.",
          "config_param": "tls-client-key-file",
          "mutable": false
        },
        {
          "name": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Whether TLS is enabled.",
          "config_param": "tls-enabled",
          "mutable": false
        },
        {
          "name": "handshake-timeout-ms",
          "type": "integer",
          "default": 10000,
          "description": "TLS handshake timeout in milliseconds.",
          "config_param": "tls-handshake-timeout-ms",
          "mutable": false
        },
        {
          "name": "key-file",
          "type": "string",
          "default": "",
          "description": "Path to the server private key file (PEM format).",
          "config_param": "tls-key-file",
          "mutable": false
        },
        {
          "name": "no-tls-on-admin-port",
          "type": "boolean",
          "default": true,
          "description": "Whether to keep the admin port as plaintext even when TLS is enabled."
        },
        {
          "name": "no-tls-on-http",
          "type": "boolean",
          "default": true,
          "description": "Whether to keep the HTTP server as plaintext even when TLS is enabled."
        },
        {
          "name": "protocols",
          "type": "array",
          "default": [
            "1.3",
            "1.2"
          ],
          "description": "Allowed TLS protocol versions.",
          "config_param": "tls-protocols",
          "mutable": false
        },
        {
          "name": "require-client-cert",
          "type": "string",
          "default": "none",
          "description": "Client certificate authentication mode.",
          "enum_values": [
            "none",
            "optional",
            "required"
          ],
          "config_param": "tls-auth-clients",
          "mutable": false
        },
        {
          "name": "tls-cluster",
          "type": "boolean",
          "default": false,
          "description": "Whether to encrypt cluster bus connections.",
          "config_param": "tls-cluster",
          "mutable": false
        },
        {
          "name": "tls-cluster-migration",
          "type": "boolean",
          "default": false,
          "description": "Whether to enable dual-accept mode for rolling TLS cluster migration.",
          "config_param": "tls-cluster-migration",
          "mutable": false
        },
        {
          "name": "tls-port",
          "type": "integer",
          "default": 6380,
          "description": "Port for TLS connections.",
          "config_param": "tls-port",
          "mutable": false
        },
        {
          "name": "tls-replication",
          "type": "boolean",
          "default": false,
          "description": "Whether to encrypt replication connections.",
          "config_param": "tls-replication",
          "mutable": false
        },
        {
          "name": "watch-certs",
          "type": "boolean",
          "default": true,
          "description": "Whether to watch certificate files for changes and auto-reload."
        },
        {
          "name": "watch-debounce-ms",
          "type": "integer",
          "default": 500,
          "description": "Debounce interval in milliseconds for certificate file watcher."
        }
      ]
    },
    "tracing": {
      "description": "Distributed tracing configuration.",
      "fields": [
        {
          "name": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Whether distributed tracing is enabled.",
          "config_param": "tracing-enabled",
          "mutable": false
        },
        {
          "name": "otlp-endpoint",
          "type": "string",
          "default": "http://localhost:4317",
          "description": "OTLP endpoint for trace export.",
          "config_param": "tracing-otlp-endpoint",
          "mutable": false
        },
        {
          "name": "persistence-spans",
          "type": "boolean",
          "default": false,
          "description": "Enable persistence spans (WAL writes, snapshots)."
        },
        {
          "name": "recent-traces-max",
          "type": "integer",
          "default": 100,
          "description": "Maximum number of recent traces to retain for DEBUG TRACING RECENT."
        },
        {
          "name": "sampling-rate",
          "type": "number",
          "default": 1.0,
          "description": "Sampling rate (0.0 to 1.0). 1.0 = sample all, 0.1 = sample 10%.",
          "config_param": "tracing-sampling-rate",
          "mutable": false
        },
        {
          "name": "scatter-gather-spans",
          "type": "boolean",
          "default": false,
          "description": "Enable scatter-gather operation spans (child spans per shard for MGET/MSET)."
        },
        {
          "name": "service-name",
          "type": "string",
          "default": "frogdb",
          "description": "Service name in traces."
        },
        {
          "name": "shard-spans",
          "type": "boolean",
          "default": false,
          "description": "Enable shard execution spans (spans inside shard workers)."
        }
      ]
    }
  }
}